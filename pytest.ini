[pytest]
# S119: pytest recolectaba test_*.py dentro de experiments/ (análisis históricos S90/S99)
# y al correr la suite sin ruta explícita los re-ejecutaba sobre data ACTUAL,
# sobreescribiendo sus outputs committeados (test_criterion.txt, test_*_result.json).
# experiments/ son snapshots reproducibles, no tests de regresión — la suite vive en tests/.
testpaths = tests
markers =
    r2_pixel_level: R2 pixel-level cross-validation vs MIROVA TIF archive (S46 — skip si TIF gap)
