name: S94 F2 reproc MODIS-only (GitHub, aislado)

# Workflow dispatch-only. F2 de S94, brazo MODIS: regenera SOLO los records MODIS
# de los 11 Tier A con la config operacional ACTUAL (perfil _s94_reproc_modis, que
# `extends: mirova_equivalent`). VIIRS va LOCAL (perfil _s94_reproc_viirs) porque
# solo MODIS requiere GitHub (pyhdf roto en Windows, regla S15). Los dos brazos se
# combinan luego por merge (store.py deduplica por (fecha, sensor)).
#
# Aislamiento (A47): data_subdir=_s94_reproc_modis, NO toca data/mirova_equivalent/.
# Matrix POR VOLCÁN (no por fecha) → cada job escribe su propio JSON, sin race.
# Promoción a operacional = decisión explícita de Nicolás tras validar (A45).
#
# A43 Norway problem: `"on":` quoted.

"on":
  workflow_dispatch:
    inputs:
      start:
        description: "Start date YYYY-MM-DD"
        required: true
        default: "2026-01-29"
      end:
        description: "End date YYYY-MM-DD inclusive"
        required: true
        default: "2026-05-29"

jobs:
  reproc:
    runs-on: ubuntu-latest
    timeout-minutes: 350
    permissions:
      contents: write
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        volcano:
          - Lascar
          - Tupungatito
          - PuyehueCordonCaulle
          - PlanchonPeteroa
          - Lastarria
          - Villarrica
          - Isluga
          - Copahue
          - NevadosDeChillan
          - Llaima
          - Chaiten
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          sudo apt-get install -y libhdf4-dev
          pip install pyhdf
          pip install earthaccess numpy h5py scipy pyyaml

      - name: Run reprocess — _s94_reproc_modis / ${{ matrix.volcano }}
        env:
          EARTHDATA_TOKEN: ${{ secrets.EARTHDATA_TOKEN }}
          EARTHDATA_USERNAME: ${{ secrets.EARTHDATA_USERNAME }}
          EARTHDATA_PASSWORD: ${{ secrets.EARTHDATA_PASSWORD }}
        timeout-minutes: 340
        run: |
          python scripts/run_pipeline.py \
            --profile _s94_reproc_modis \
            --volcano ${{ matrix.volcano }} \
            --start ${{ github.event.inputs.start }} \
            --end ${{ github.event.inputs.end }} \
            --overwrite

      - name: Commit reprocessed validation data
        run: |
          set +e
          git config user.name  "vrp-bot"
          git config user.email "vrp-bot@github-actions"
          git add "data/_s94_reproc_modis/${{ matrix.volcano }}.json" 2>/dev/null
          git diff --staged --quiet && { echo "No changes to commit"; exit 0; }
          git commit -m "S94 F2 — reproc MODIS-only ${{ matrix.volcano }} _s94_reproc_modis ${{ github.event.inputs.start }}->${{ github.event.inputs.end }}"
          for attempt in 1 2 3 4 5 6; do
            git pull --rebase -X theirs origin main && git push && exit 0
            echo "push attempt $attempt failed, sleeping $((attempt * 10))s"
            git rebase --abort 2>/dev/null
            sleep $((attempt * 10))
          done
          echo "all push attempts failed"; exit 1
