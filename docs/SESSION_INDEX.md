# Session Index VRP Chile

> Resumen 1-2 líneas por sesión con hallazgo principal. Mantener cronológicamente.

| S# | Fecha | Hallazgo principal | Artefacto |
|---|---|---|---|
| S1-S8 | 2026-01 a 2026-03 | Bootstrapping pipeline: fetch VIIRS+MODIS, estructura YAML volcanes, NTI path + vent path simple, 11 volcanes Tier A | `pipeline/` inicial |
| S9 | 2026-03 | Vent-path con threshold fijo 1K (no sigma-gating): recall alto Tupungatito 0.977, Chaitén 0.929. FPs no-filtrados | commit `ecb5d66` |
| S10 | 2026-03 | `ENABLE_VENT_PATH_MODIS=false` para bajar 21% FPs en Lascar — causó regresión MODIS 0.83→0.058 | commit `56a1318` |
| S11 | 2026-04 | Path C NTI relativo en experimental | commit `2ee346e` |
| S12 | 2026-04-12 a 2026-04-16 | **Sigma-gating vent-path** `max(1K, 2σ)` → "elimina 85% FPs" pero mata TPs sub-pixel (Tupungatito 0.977→0.5, Chaitén 0.929→0.5). Matrix 45 volcanes, NRT cron 2h | commits `6eaed67` (F1), `4c80429` (F1b cap 3K), `39e6bb0` (pisos VRP) |
| S13 | 2026-04-17 | Plan integrated-ROI para Villarrica sub-pixel | `tasks/plan_s13_test1_integrated_roi.md` |
| S14 | 2026-04-18 a 2026-04-20 | Fix geometría MIROVA-equivalent: `radius_km=25` uniforme, `inner_radius_km` per-volcán de KML oficial. Schema unificado `final_hotspot_*`. **Validación empírica coeficientes Wooster** contra OSF v2.5: error ≤0.17% en 48,360 filas. Coeffs: MODIS=18.9, VIIRS M=19.7, VIIRS I=18.0 | `experiments/21_results.json`, commit `5478bce` |
| S15 | 2026-04-21 a 2026-04-22 | **4 fixes arquitecturales**: (P3.2) dNTI contextual 8-vecinos Path D; (P3.1) dual-ROI thresholds summit/scene; (Tema E) bbox 50×50 km reemplaza círculo; (Tema F) sigma-cap eruption-path VIIRS=7K. Reproceso background pendiente validación. **NO pusheado a main** | `tasks/handoff_mananero_2026_04_22.md` |
| S16 | 2026-04-23 (primera mitad) | Diagnóstico H1 con `git show 6eaed67` confirmado: commit S12 F1 causó regresión. Profile `s9_vent_permissive` creado con `n_sigma_vent=0`. **Bug bbox: Salar de Atacama (Lascar) y Embalse El Yeso (Tupungatito) capturados** como FPs 200-1500 MW. **P3.6 exclusion zones** implementado en `pipeline/exclusion_zones.py` + `volcanoes.yaml`. Reproceso E1+P3.6 pendiente | commits `d461f4a`, `6f87989`, handoff `tasks/handoff_s17_2026_04_23.md` |
| **S17** | **2026-04-23 (segunda mitad)** | **Investigación sistemática con 4 skills invocadas + 11 agentes paralelos**. Hallazgos: **(1) fix performance Path D factor 2400×** (commit `ad030f5`); **(2) H1 sigma-gating REFUTADA** — E1 no mueve recall; **(3) H10 CONFIRMADA — NOAA-21 missing en fetch.py** es el cuello real; **(4) 3 drifts detectados en código vs papers**: median→mean, N·σ=3→5-10-15, TIR Stefan vs Aveni. **Auditoría 10 papers** Coppola/Campus/Wooster/Di Bella/Aveni consolidada. **Arquitectura de memoria instalada**: docs/DRIFTS, PAPERS_AUDIT, DATA_SOURCES, HYPOTHESIS_LOG, SESSION_INDEX | docs/ completo + commit `ad030f5` |
| **S18** | **2026-04-24** | **H10 IMPLEMENTADA y VALIDADA empíricamente**. (1) **Integración NOAA-21** (VJ202IMG/VJ203IMG/VJ202MOD/VJ203MOD v2.1) en fetch.py + sensor labels en process_viirs*.py + piso store.py + dispatch run_pipeline.py. 15 tests TDD nuevos (65/65 verde). (2) **Reproceso 3 volcanes Tier A** ventana 2026-04-08→04-22 (local paralelo, 45-75 min). (3) **Recall H10 validado contra MIROVA NRT**: Lascar 0.52→**0.86** (+14 TP), Chaitén 0.50→**1.00** (+1 TP), Tupungatito 0.24→**0.41** (+3 TP). H10 confirmada — NOAA-21 agrega TPs reales. Tupungatito queda bajo 0.77 del handoff → **H17 nueva** (Embalse El Yeso / σ_bg) para S19. (4) **Dashboard refactor mayor** a pedido del geólogo: fix FP 1.5M MW Lastarria (granule MODIS dañado, 357 pixels BT=566K), archivo histórico de 34 no-Tier-A a `data/archive/` + 12 `_OLD_*` via `git mv` (preserva historia git), filtro a 11 Tier A + 45 cards con "Sin datos", conteos 🎯/📍 7d, toggle "Solo cráter / Incluir lejanas" (default cráter), tabla con columna Zona + atenuación filas far, 3 radios sincronizados con YAML (25 km + inner_radius_km 3-20 km + vent_radius_km 2-5 km), fix distanceCounts con fallback vrp_vent>0. (5) Infraestructura A/B drift D2 instalada (profiles nsigma_mir_5/12 + experiments/37) pero ejecución diferida S19. (6) Merge `s15-dev→main` con 68 conflictos resueltos (34 UD no-Tier-A archivados, 34 UU experimental accept HEAD, 7 UU Tier A: 3 reproceso / 4 NRT) | commits pipeline `b08b71f`, reproceso `80bc302`, merge `f78ad5d`. Dashboard: `cd8e8e5`/`a0d6053`/`15f6849`/`9501d58`/`397b55f`/`47b9fd1` |
| **S20** | **2026-04-25 (tarde)** | **Forense H17 + Regla D vent-priority RESUELVE T3 (8/15 FN Tupungatito)**. (1) **Forense H17**: clasificadas 35 refs MIROVA Tupungatito summit en 30 días. 20 TP + 15 FN. De los 15 FN: 8 son **T3** (vent-path SÍ detectó cráter pero record clasificado far), 9 son **T4** (no hay pixel summit detectado en absoluto), 4 son T2b (relacionado), 2 son T1 (no granule). (2) **Regla D vent-priority**: si vrp_vent_mw>0, forzar distance_class=summit y final_hotspot=vent. Espíritu S9 (recall 0.98) traducido a arquitectura S14+ con clasificación espacial. **5 tests TDD nuevos** (suite 83/83). Aplicada retroactivamente a 11 Tier A: 1,574 records reclasificados (Tupungatito 310, PCC 354, Chaitén 221, Lascar 255, Lastarria 166, Isluga 112, Llaima 38, Planchón 36, NdC 31, Villarrica 30, Copahue 21). (3) **Validación empírica recall summit-class**: agregado 0.25→**0.69 (+0.44)**. Chaitén 0.50→**1.00 ✅ supera S9** (0.93). Lascar 0.35→0.73. Tupungatito 0.00→0.57 (sigue lejos de S9 0.98 — T4 explica). (4) **Hallazgo nuevo D6 — background no localizado**: los 13 T4 Tupungatito tienen pixels detectados (n_anom 1-772) pero TODOS far porque `std_bg` global se infla por glaciar lateral, capeando vent threshold a 3K. ΔT real cráter ~1.5-2K no dispara. MIROVA usa background localizado (Coppola 2016a Tabla 1: ROI1 5×5km vs ROI2 50×50km). Fix requiere `std_bg_summit` calculado solo sobre ROI1 + reproceso. **Diferido S21**. | commits regla D `2fde274`, merge main `9b8d852` |
| **S19** | **2026-04-25 (mañana)** | **Auditoría meta + M1-M4 mecanismos seguridad + D2 RESUELTO**. Sesión empezó con paso atrás metodológico pedido por Nicolás. (1) Mecanismos instalados: **M1 golden tests** anti-regresión (4 records canónicos, 8 tests, total 73/73 verde); **M2 verify_reproc.py** chequeo automático post-reproceso (calibración NOAA-21 OK, exclusion_zones, records sospechosos, cobertura sensor); **M3 badge NRT + workflow alerta automática** issue si 3 corridas fallan; **M4 sanity cap físico** vrp_mw=50,000 MW derivado del P99.99 OSF v2.5 (5 tests TDD, 78/78 verde). M5 FIRMS cancelado por scope creep (redundante con MIROVA, mismo satélite). (2) **Auditoría imágenes MIROVA** publicadas en mirovaweb.it: 36 imágenes (12 por volcán Tup/Cha/Las × 3 sensores × 4 tipos). Hallazgos: MIROVA reporta 99% de detecciones a ≤5 km (CSV); las imágenes Last Year cubren 12 meses con puntos rojos <inner / negros >inner. **Truncamiento `floor()` confirmado** en imágenes (1.7→"1"); usar SOLO el CSV para magnitudes exactas. (3) **Workflow NRT filtrado a 11 Tier A**: `mirova_equivalent` step solo corre para los 11 Tier A vía `contains(fromJson(...))`, los 34 restantes solo en experimental. ~50% menos minutos Actions. (4) **POI marker dashboard** (-33.28, -69.58) hotspot NE Tupungatito — Nicolás verificó visualmente que NO hay nada → H17a descartada, H17 sigue activa. (5) **D2 RESUELTO**: A/B 3σ vs 5σ vs 12σ, 30 días, 3 volcanes. **Hallazgo clave**: cap `MAX_SIGMA_COMPONENT_K=7K` ([process_viirs.py:358](../pipeline/process_viirs.py#L358)) anula la diferencia 5σ vs 12σ cuando `std_bg > 0.58 K`. **3σ baseline gana en F1 (0.36 vs 0.29)**. El cap implementa de facto un umbral adaptativo que combina lo mejor de Coppola y Di Bella sin penalizar volcanes con σ_bg alto. **Decisión**: mantener 3σ + cap. **H17 no resuelve por A/B** — no es N·σ, es geográfico/sub-pixel. Camino S20: dual-ROI Coppola 5σ summit / 10σ scene | commits M1-M4 `2d29de1`/`1a661c2`. NRT filter `9254451`. POI marker `1fc2501` |
| **S23** | **2026-04-26 (madrugada-mañana)** | **Audit profundo S22 + Fases 1-3 ejecutadas**. (1) **Audit con 5 agentes paralelos** detectó 18 hallazgos (4 críticos, 7 altos, 5 medios, 2 bajos): divergencia MODIS-VIIRS local ROI threshold, Regla D KeyError potencial, process_modis sin tests, factor 42 clustering, E33 reproceso P3.1 missing, VRP_TIR Aveni 2025, Di Bella k_VIIRS, haversine None, P95 hardcoded, 10 papers Vault sin auditar, etc. (2) **Plan formal 22 tasks** en `docs/superpowers/plans/2026-04-26-s23-audit-followup.md`. (3) **Fase 1 — bugs latentes producción**: T1 Regla D edge case fix con preservación comportamiento legacy S20 (commit `7a0649a`); T2 haversine_km centralizado a scan_geometry + TypeError None guard (`1236b8a`); T3 **D7 documentado refinado** — hallazgo nuevo: solo VIIRS 375m carece del filtro local p95 (MODIS y VIIRS 750m sí lo tienen, pixels I-band 1/4 área), `acb98ff`. (4) **Fase 2 NRT validación pasiva**: Run NRT 10:35 UTC con fixes S22 → Lastarria push exitoso 10:57 UTC (fix git push + H6 funcionan en producción). (5) **Fase 3 cobertura tests masiva**: T5 process_modis.py 5 tests core (commit `2622579`); T6 store.append_record 5 edge cases (`1e161b7`); T7 scan_geometry.py 13 tests sec³+areas+bbox (`4b5da1c`); T8 M1 golden expand 4→8 records canónicos (`e7ccf06`). **Suite tests 119 → 164 (+45 S23). 100% verde**. Fases 4-7 (asunciones, papers Vault, factor 42, Di Bella, VRP_TIR, mejoras operacionales) diferidas S24+ con plan formal y handoff. | 12 commits S23 (incluyendo `7a0649a`/`1236b8a`/`acb98ff`/`2622579`/`1e161b7`/`4b5da1c`/`e7ccf06`) |
| **S22** (parcial) | **2026-04-25/26 (noche-madrugada)** | **S22.1 RESUELTO — Schema VIIRS paridad MODIS (H_S21_11)**. (1) `process_viirs.py` y `process_viirs_mod.py` ahora retornan los 10 campos `diag_*` (paridad con `process_modis.py`): `diag_sigma_bg_k`, `diag_eff_threshold_k`, `diag_t_max_dist_km`, `diag_roi_p95_k`, `diag_nti_bg/std/max`, `diag_n_bt_path/nti_path/dnti_ctx_path`. Cómputos extra ~6 líneas usando variables locales ya disponibles. Algunos campos duplican existentes (`nti_bg`, `nti_max`, `n_*_path`) preservados por compat frontend. (2) **3 schema-source tests TDD** verde + suite total 119/119. (3) **Smoke test reproceso 1 día Tupungatito** (2026-04-22) validó campos poblados con valores físicamente coherentes (sigma=3.1K, eff_threshold=271K abs, p95=272K, etc). (4) **Fix BT absoluto** detectado en smoke: `diag_eff_threshold_k` ahora es BT absoluto (`t_bg + delta`) para paridad real con MODIS. (5) **Auth netrc fallback** en `pipeline/fetch.py auth()`: trata `environment` primero, cae a `netrc` si falla. Permite correr local sin env vars. (6) NRT cron poblará records nuevos con diag_* gradualmente; reprocesos historicos diferidos a S22.3 si necesarios. **S22.2 pendiente**: A/B test cap `MAX_VENT_SIGMA_CONTRIB_K` 3→2 K ahora habilitado porque hay schema para diagnóstico TP vs T4. | commits `f42f69b` (schema VIIRS), `b9ccfa8` (BT absoluto + auth fallback) |
| **S21** | **2026-04-25 (noche)** | **D6 REFUTADO empíricamente + arquitectura de persistencia operacionalizada + Fase 1 diagnóstico completo**. Sesión arrancó con paso atrás metodológico (auditoría sistemática + 4 agentes paralelos + writing-plans). **Fase 0 — persistencia (5 commits)**: (1) `memory/project_s21_findings.md` con 11 hallazgos H_S21_1..11 documentando schema gaps, MODIS Tupungatito vacío, distancias CSV reales, ground truth Mirova-v1, OCR outliers, etc; (2) `docs/SESSION_CLOSE_CHECKLIST.md` operacional bloques A-F (causa raíz: trigger CLAUDE.md sin checklist falló S20→S21); (3) `CLAUDE.md` trigger cierre apunta al checklist + regla meta-meta S21 "persistencia in-vivo no al cierre" + 15 repos del ecosistema; (4) commits evidencia: 36 PNGs mirovaweb, A/B nsigma S19, snapshot Mirova-v1; (5) Vault desacople resuelto: 26 papers MIROVA con `proyecto: "[[VRP Chile]]"` + dataview query (sin mover archivos físicos). **Fase 1 — diagnóstico empírico D6 (3 experiments TDD)**: (6) `experiments/forense_h17_replicable.py` (12 tests) — TP/T1/T2b/T3/T4 reproducible, válida narrativa S20 al bit; (7) `experiments/locate_active_vent.py` (8 tests) — centroide ponderado VRP; (8) `experiments/measure_local_bg_offline.py` (8 tests) — descarga 3 granules T4 Tupungatito vía earthaccess y mide std_bg multi-ROI **directamente sobre data raw**. **D6 REFUTADO**: ratio summit_5_8/global = 0.81 (esperaba <0.5). El glaciar Tupungatito afecta toda el área hasta 10+ km, no solo el anillo lateral. Cap MAX_VENT_SIGMA_CONTRIB_K=3K satura igual con std_bg local ≈4.5K que con global ≈5.5K. Causa raíz reformulada: fumarola sub-pixel + sub-Kelvin con variabilidad nocturna. **Hallazgo crítico H_S21_11**: `process_viirs.py` no guarda `diag_*` campos (sigma, eff_threshold, n_bt, n_nti, n_dnti_ctx) — gap de schema VIIRS bloquea TODA validación posterior sin reproceso. **Plan S22 prioritario**: agregar `diag_*` al schema VIIRS (paridad con MODIS). Antes de eso cualquier mejora de recall Tupungatito es a ciegas. Suite tests: 83→124 (+41 tests S21 verde). Commits: `cc19390` (cierre+plan+CLAUDE.md), `4381ccc` (PNGs), `5145e17` (A/B), `d07b877` (snapshot), `1c60660` (Vault), `5a5bd18` (forense), `0dcba65` (locate vent), `66466f9` (analyze sigma), `a87c261` (measure local bg + diagnosis final) |

---

## Glosario de sesiones (convenciones)

- **Sesión** = un bloque de trabajo focused Claude Code + Nicolás, con handoff al final.
- **Numeración**: S1 = bootstrapping; S17 = 2026-04-23 PM.
- **Handoff**: archivo en `tasks/handoff_s##_YYYY_MM_DD.md` que resume estado al cierre.

---

## Próximas sesiones planeadas

### S18 — Fix drifts + NOAA-21
- Corregir D1 (mean vs median) en `detection_context.py` con TDD.
- Agregar NOAA-21 a `fetch.py` + `process_viirs.py` + `process_viirs_mod.py` con TDD.
- Reproceso 3 volcanes Tier A (Tupungatito, Chaitén, Lascar) con pipeline ampliado.
- Test A/B para drift D2 (N·σ): 3 configs vs OSF v2.5.
- Si valida → push main.
- Auditar Coppola 2022 Sabancaya (s00445-022-01523-1.pdf).

### S19 — ✅ Cerrada (ver fila S19 arriba)
- M1-M4 mecanismos de seguridad instalados.
- D2 resuelto (mantener 3σ + cap).
- Workflow NRT filtrado a 11 Tier A.
- Truncamiento imágenes MIROVA documentado.

### S20 — ✅ Cerrada (ver fila S20 arriba)
- Forense H17 completa (T1/T2b/T3/T4 clasificados).
- Regla D vent-priority implementada + aplicada retroactivamente.
- Recall agregado +0.44.
- Hallazgo D6 (background no localizado) documentado para S21.

### S21 — Background localizado (D6) — el fix grande para Tupungatito
**Objetivo**: implementar `std_bg_summit` calculado solo sobre ROI1 (5×5 km
del cráter) en lugar del anillo bbox global. Eso debería:
- Bajar el threshold vent en volcanes con glaciar lateral (Tupungatito, Villarrica) sin afectar volcanes de cielo limpio (Chaitén).
- Resolver los 13 T4 Tupungatito (recall 0.57 → ~0.95, cerca de S9 0.98).

**Plan tentativo**:
1. Spec con `superpowers-brainstorming`: ¿reemplazar std_bg actual o agregar como segundo std_bg dual-ROI?
2. TDD en `process_viirs.py`: nuevo `compute_local_bg_stats(bt, lat, lon, vol_lat, vol_lon, inner_km)` retornando (t_bg_summit, std_bg_summit).
3. Integrar al gate vent-path con threshold = max(1K, min(2·std_bg_summit, 3K)).
4. Reproceso 3 volcanes (Tup/Cha/Las) con perfil nuevo `local_bg`.
5. Comparar contra baseline S20 (que ya tiene regla D aplicada).
6. Si Tupungatito sube a >0.85 sin degradar Lascar/Chaitén → adoptar.

**Riesgos** (importante revisar antes de implementar):
- Si ROI1 5×5km tiene < 25 pixels válidos en algunos granules, std_bg_summit puede ser ruidoso.
- Volcanes con cráter activo (Lascar) tienen pixels HOT dentro de ROI1 que contaminarían el cómputo de bg local — necesitamos excluir el vent_radius del cómputo bg.

### S22+ — TIR + Feature parity + SWIR
- Auditar Aveni 2024 TIRVolcH RSE → confirmar D3.
- Feature parity dashboard: Combined MIR series + escala alerta visible.
- SWIR pipeline Massimetti 2020 (Sentinel-2 MSI + Landsat-8/9 OLI) — scope grande.
- `scripts/preflight_cmr_coverage.py` — chequeo CMR antes de afirmar "sin data".
- Investigar granule MODIS Lastarria dañado (BT=566K) — requiere pyhdf en Linux.
