# Verificador con contexto limpio: pre-registro del A/B D22/D25 (S143)

> Auditado: `docs/PREREGISTRO_AB_D22_D25_S143.md` (borrador v1, 2026-09-17). Verificador escrito el
> 2026-09-17 sin tocar ningún otro archivo del repo, sin git y sin despachar nada. Los dos scripts
> de comprobación que corrí se copiaron después a
> `experiments/_s143_preregistro/verificador/` (`proxy_c1.py`, `dryrun_s135.py`); lo que dicen se resume abajo con su comando.

## Veredicto

**CORREGIR ANTES.** El brazo `literal` no quita la compuerta en la máscara que produjo las 12
pérdidas de S135, así que la prueba de H1 puede salir refutada por una razón que el documento no
declara; y el evaluador que aplicaría los criterios no existe y los scripts citados no sirven tal
como están.

## Hallazgos

Gravedad 5 = el A/B mediría otra cosa que la que dice; 1 = redacción.

| # | grav. | estado | hallazgo | evidencia | corrección propuesta |
|---|---|---|---|---|---|
| 1 | **5** | CONFIRMADO | **El brazo `literal` conserva la compuerta de temperatura en la ruta del Test 1, que es justo la ruta de las 12 pérdidas.** El filtro contextual del Test 1 intersecta `test1_hot_filtered` con `dnti_ctx_hot`, y esa máscara se calcula con `dual_roi_contextual_dnti_hot_mask(...)` sin pasar `apply_bt_gate`, cuyo default es `True`. Por decisión del dueño (plan, ajuste S142 punto 2) D22 NO se cableó en el camino D. El documento dice "D22 sin compuerta: on" para `literal` sin salvedad, y H1 afirma que "sin la compuerta, el primer pase conserva los píxeles del cráter". El primer pase sí pierde la compuerta, pero el filtro del Test 1 no mira el primer pase. | `pipeline/process_viirs.py:1063-1072` (llamada sin `apply_bt_gate`); `pipeline/detection_context.py:345` (`apply_bt_gate: bool = True`); `process_viirs.py:1839-1850` (intersección S99 con `dnti_ctx_hot`); `pipeline/test1_contextual_filter.py:57` (`out = test1_mask & dnti_ctx_mask`); `VRP_PROFILE=_s142_ab_literal` da `ENABLE_DNTI_CONTEXTUAL_PATH=True`, `NTI_BT_SANITY_K=3.0`; `docs/superpowers/plans/2026-09-15-flags-d22-d25.md` ajuste S142 punto 2; `experiments/_s135_ab_d1d2/RESULTADO_FINAL.md:53-56` (la pérdida es la intersección con la máscara contextual); `docs/HYPOTHESIS_LOG.md:1510` (probe v2: en la ruta del Test 1 "la compuerta de BT en 18 de 18") | Declararlo en §1, §3 y §9. Reescribir H1 con lo que el brazo puede probar: "quitar la compuerta del primer pase y cambiar el fondo recupera las 12 noches". Decidir ANTES de correr, con Nicolás, si (a) se acepta que el A/B no puede refutar la compuerta en la ruta del Test 1 (y un resultado negativo no se atribuye a "la compuerta no era"), o (b) se agrega un brazo que la quite también en `dnti_ctx_hot` (código nuevo, A45). Sin esto, "literal pierde alguna de las 12" refutaría H1 por una compuerta que sigue puesta (A97). |
| 2 | **4** | CONFIRMADO (código); SOSPECHA (tamaño en los brazos) | **El filtro de mismo objeto sólo se aplica al control, no al brazo.** `evaluar_ab.py` filtra por cota A93 las noches del control, pero para el brazo cuenta como "publica" cualquier record de esa fecha. Un brazo sin compuerta que publique ruido topográfico (A69) en otra posición del disco interior "retiene" la noche. Es el riesgo que H1 dice medir a la par. En los records guardados, las noches con alerta donde lo publicado no pasa la cota no son raras. | `experiments/_s135_ab_d1d2/evaluar_ab.py:268-283` (filtro sobre control) contra `:317` (brazo: `{fecha for r if publica_en_crater}`). Proxy sobre `data/mirova_equivalent` (régimen mixto, referencia de `_dl_referencia`): Lastarria 46 noches con alerta publicadas, 36 con objeto dentro de la cota, 10 sólo con objetos fuera; Isluga 4, Láscar 5, PCC 5 (`proxy_c1.py`) | Pérdida = noche confirmada en la que el brazo no publica **un objeto que pase la misma cota** contra las distancias de MIROVA de esa noche. Reportar además la versión sin filtro en el brazo, para ver cuántas noches se "retienen" con otro objeto. |
| 3 | **4** | CONFIRMADO | **El evaluador que aplica los criterios no existe, y los scripts citados no corren sobre este universo.** `banco_paridad.cargar_nuestros` lee de `DATA` fijo y de los 11 `VOLS`: sobre un directorio de brazo con 8 volcanes falla con archivo inexistente. `evaluar_ab.py` tiene `INNER` sólo para 6 volcanes (Villarrica y NdC dan `KeyError`), `FOCALES = {Lascar, Lastarria}` (otra partición), predicado reconstruido a mano (A97), referencia del snapshot local y pareo a ±20 min. `fusionar_chunks.py` y `evaluar_ab.cargar` tienen fijos los nombres `s135ab-`/`_s135_ab_*`. Escribir el evaluador después de ver datos deja grados de libertad que el pre-registro dice no tener. | `scripts/banco_paridad.py:67-70, 237-241`; `evaluar_ab.py:78, 89-93, 118, 137-144, 181-182`; `fusionar_chunks.py:25-35` | Escribir y testear el evaluador ANTES de despachar, commitear y citar su sha en el pre-registro. Control del instrumento obligatorio: correrlo sobre los artefactos de S135 (`experiments/_artefactos_ab/s135ab-*`, locales) y exigir que reproduzca las 260 noches confirmadas y las 12 pérdidas del brazo D. Es factible: mi corrida mínima (`dryrun_s135.py`) con el predicado node reproduce las 12 como perdidas en D y publicadas en A y B. |
| 4 | **3** | CONFIRMADO | **Criterio 3: las medianas se toman sobre conjuntos de pasadas distintos en cada brazo**, y el umbral n ≥ 30 no dice de quién es el n. Un brazo que deja de publicar las pasadas débiles mueve la mediana sin que ninguna pasada se acerque a MIROVA; y un volcán cuyo brazo cae bajo 30 pares escapa al chequeo justo cuando pierde publicación. Tampoco se dice qué VRP de MIROVA usar cuando hay fila CONS y OCR en la misma pasada (`parear` devuelve todas). | Corrida mínima sobre artefactos S135: pasadas `pos` publicadas, control 498 de 498, brazo D 425 de 498 (`dryrun_s135.py`); `banco_paridad.py:225-233`; §5 criterio 3 del documento | Decisivo sobre las pasadas `pos` que publican **ambos** (control y brazo); informativo sobre el conjunto de cada brazo. n ≥ 30 medido sobre las `pos` del control. Fijar la fila de MIROVA (por ejemplo CONS si existe, si no OCR). |
| 5 | **3** | CONFIRMADO (lectura) | **H1 se contradice con su propia tabla y su regla de refutación es incompleta.** H1 dice que el fondo de vecinos es lo que evita que la energía se recorte a cero; entonces `lit_sin_fondo` debería perder noches, pero la tabla le predice 0. La refutación sólo mira `literal` y `lit_con_compuerta`, no `lit_sin_fondo`, y H1 está escrita como conjunción ("de la compuerta y del fondo") mientras la refutación trata a la compuerta como necesaria. | Documento l. 40-43 contra l. 50 y l. 55-56 | Escribir H1 por factor (H1a compuerta, H1b fondo), con una predicción y una condición de refutación por ablación, coherentes entre sí. |
| 6 | **3** | CONFIRMADO | **§1 describe mal dos mecanismos.** (a) "En la figura A6 esa compuerta elimina el cráter de Villarrica (S137)": el catálogo corrigió esa atribución en S138, la compuerta no es lo que pierde a A6, es el fondo. (b) "Usamos la mediana de un anillo regional de 5 a 25 km": en el bloque contextual, 4 de los 8 volcanes (Lastarria, Planchón-Peteroa, Puyehue-Cordón Caulle, Villarrica) usan el kernel 3x3 local; en la ruta del Test 1 el fondo es el anillo intermedio de 1,5 a 3 km (Láscar, Lastarria, NdC) o el fondo local. Lo que `lit_sin_fondo` ablaciona no es lo mismo en cada volcán. | `docs/MIROVA_DIVERGENCES.md:2246-2248`; `volcanoes.yaml` `local_kernel_bg: true` en PCC, Villarrica, Chaitén, PP, Lastarria; `process_viirs.py:1428-1450`; `pipeline/test1_integrated.py:147-153`; perfil control: `ENABLE_TEST1_INTERMEDIATE_BG=True`, `ENABLE_TEST1_LBG_GLOBAL=True`, `lbg_global_compatible` en Láscar, NdC, Lastarria | Corregir §1 y agregar una tabla "fondo del control por volcán y ruta" para interpretar las ablaciones. |
| 7 | **3** | CONFIRMADO (lectura) | **La investigación de pérdidas es una puerta trasera.** Criterio 1 dice que una pérdida abre investigación con tres explicaciones posibles, pero no dice si una pérdida explicada deja de contar. Dos de las tres ya están excluidas por construcción (diurnas por `is_nighttime`, entrada distinta por cobertura pareja), así que la única salida sería reclasificar después de ver. | Documento §5 criterio 1 y §6.1; `docs/PREREGISTRO_AB_D1_D2_S135.md` casos 2 y 3 ("no cuenta como pérdida") | Declarar ahora: toda pérdida cuenta para el criterio 1; la investigación informa el siguiente paso pero no cambia el conteo. Cualquier regla de exclusión se escribe hoy y se aplica igual a todos los brazos. |
| 8 | **3** | CONFIRMADO (lectura) | **La desviación del criterio 4 es honesta en parámetros pero no en hipótesis.** No hay parámetros que ajustar, cierto. Pero H1 y la elección de ablaciones nacieron de las 12 noches perdidas de esta misma ventana (S135), así que el chequeo de H1 es en muestra. El spec pide prueba en junio a septiembre y el documento deja fuera septiembre, que ya existe y está en el régimen post #571. Además, "si se ajusta algo se juzga desde el 2026-09-01" choca con el vencimiento del token el 2026-10-03. | Spec `2026-09-13-plan-definitivo-paridad-design.md` §5.4; documento §5 criterio 4 y §8; `tasks/BLOQUE_ARRANQUE_S143.md` §b.1 | Declarar el chequeo de las 12 noches como en muestra. Agregar un tramo de confirmación 2026-09-01 a 2026-09-15 (fuera de muestra, régimen actual) que la adopción también deba pasar, o escribir por qué no. |
| 9 | **3** | CONFIRMADO | **Desviaciones no declaradas.** (a) El spec §4 dice "A/B con los brazos que el probe justifique" y el probe v2 quedó "verificado, NO justifica A/B"; el documento no lo menciona. (b) El traspaso S143 pedía además "el brazo que apaga el recorte a cero"; el documento lo manda a §7 sin decirlo. (c) El plan (ajuste 3) dice que el parámetro existe "para que el A/B barra 1 contra 3"; §9 dice que no se barre. (d) El spec §5.1 mide la pérdida "con cualquier sensor"; el documento la mide sólo en VIIRS 375 (más estricto, pero es otro criterio). | Spec §4 Fase 1 y §5.1; `tasks/BLOQUE_ARRANQUE_S143.md:15, 69, 96`; plan ajuste S142 punto 3 | Sección "Desviaciones declaradas" con las cuatro y su razón. |
| 10 | **3** | CONFIRMADO (predicado); SOSPECHA (efecto) | **El predicado "publica" incluye la supresión de artefactos del display.** Un brazo sin compuerta que genere más campos difusos o cirrus (A69) puede "bajar la publicación" porque el dashboard los esconde, no porque el algoritmo deje de generarlos (A72). | `scripts/banco_paridad.py:124-128` (`!art` dentro de `pub`) | Reportar como acompañante obligatorio la tasa de `art` y la tasa previa al display (`summit && valid && disp > 0`) en negativos limpios; si `art` sube en el brazo, decirlo en el veredicto del criterio 2. |
| 11 | **3** | CONFIRMADO | **Código y referencia sin fijar.** El workflow plantilla hace checkout sin `ref`, y en S135 el tramo 2 se despachó con el tramo 1 corriendo (09:06 contra fin 15:03 del 08-sep): si entra un PR a `pipeline/` entre tramos, los dos tramos corren con código distinto. La referencia de §4 se fijó por sha, pero el criterio 1 no dice con qué referencia se evalúa (el OCR se completa con retraso). | `.github/workflows/reproc-s135-ab-d1d2.yml:74-75`; `gh run view 34173711390/34208191011` (created/último completed); `experiments/_s143_preregistro/denominadores.json` meta.referencia | Tag defensivo y `gh workflow run --ref <tag>` en ambos tramos; el evaluador baja CONS y OCR por los dos sha de `denominadores.json`. |
| 12 | 2 | CONFIRMADO | **La justificación de los ocho volcanes no calza con los datos.** "Los focales de S135 casi no tienen negativos limpios (Isluga 21)": PP tiene 145 y PCC 146, ambos focales en la partición del propio documento. Chaitén queda fuera "por costo" pero tiene más noches con alerta (14) y más negativos (221) que Villarrica (11, 218) y NdC (5, 198); en el proxy de noches confirmadas, Chaitén 12, Villarrica 9, NdC 3. Y Chaitén es el volcán cuya magnitud ya se pasa arriba (1,35), el contraejemplo natural para un D25 que sube la magnitud. | `denominadores.json`; `proxy_c1.py`; `tasks/BLOQUE_ARRANQUE_S143.md` §d ("Chaitén se pasa arriba (1,35)") | Corregir el texto. Sólo la razón A69 sostiene a NdC; considerar Chaitén en lugar de, o además de, Villarrica. |
| 13 | 2 | CONFIRMADO | **Dos particiones focal/nevado en el código.** El documento usa `build_c2ab_windows.py:41-42` (lo manda el spec §5.1), pero el A/B de S135 que cita usaba `FOCALES = {Lascar, Lastarria}` y reportó "focales 2, nevados 10"; con la partición nueva las mismas 12 son 10 focales y 2 nevadas. El propio script dice que la partición es "solo para anotar", y A21/A12 ponen a PP e Isluga en la clase de ΔT bajo. | `evaluar_ab.py:91`; `resultado_final.json` `focales_vs_nevados`; `scripts/build_c2ab_windows.py:40` | Declarar el cambio de etiqueta respecto de S135 y dar siempre el desglose por volcán junto al del estrato. |
| 14 | 2 | CONFIRMADO | **Bootstrap del criterio 2.** Remuestrear noches del total mezclado cambia la composición por volcán en cada réplica; el total queda dominado por el estrato nevado (538 de 952 pasadas). "No positiva en ningún estrato" sin tolerancia hace que una sola pasada decida. | `denominadores.json` (suma de `pasadas_neg_limpio`) | Bootstrap estratificado por volcán (remuestrear noches dentro de cada volcán). Mantener la regla por estrato pero reportar cuántas pasadas la deciden. |
| 15 | 2 | CONFIRMADO | **El control de cobertura del patrón es asimétrico**: sólo cuenta pasadas que el control tiene y el brazo no; pasadas extra del brazo pasarían como ganancias. Tampoco compara `product_version`. | `evaluar_ab.py:240-241` | Diferencia simétrica por `(datetime_utc, sensor)` y mismo `product_version`. |
| 16 | 2 | CONFIRMADO | **Falta un control de instrumento sobre la línea base.** Los records guardados publican en negativos limpios 459 de 952 (48 %, régimen mixto); el control reprocesado de S135, en 6 volcanes, 502 de 535 (93,8 %); la línea base post #571 es 87,1 %. Si el control nuevo no cae en ese régimen, algo del reproceso o del evaluador está mal. | `denominadores.json`; `dryrun_s135.py`; `tasks/BLOQUE_ARRANQUE_S143.md` §a | Declarar el rango esperado del control y qué se hace si no cae. |
| 17 | 2 | CONFIRMADO | **Riesgos de ejecución no escritos**: fusión de tramos con script nuevo (no dice fechas de corte ni `overwrite`); no superponer tramos (6 + 6 + 8 del NRT se acerca al tope de 20 jobs); anotar en `HYPOTHESIS_LOG.md` obliga a correr la suite (un test lo lee, lección S142); retención de artefactos 14 días. | `reproc-s135-ab-d1d2.yml:32-51, 128`; `tasks/BLOQUE_ARRANQUE_S143.md` §c.2 | Agregar a §8. |
| 18 | 1 | CONFIRMADO | La columna "noches con alerta nocturna de MIROVA" está bien rotulada (sale de la referencia), pero el universo del criterio 1 será menor. Proxy sobre guardados con el filtro de S135: Isluga 70, Láscar 55, Lastarria 36, PP 27, PCC 30, Tupungatito 26, Villarrica 9, NdC 3. NdC entra al criterio 1 con 3 noches. | `proxy_c1.py` (proxy, régimen mixto) | Agregar la columna proxy o una nota. |

## Lo que comprobé y SÍ está bien

- **Denominadores de §4**: los 16 números de la tabla coinciden con `denominadores.json` (noches con alerta de referencia y `pasadas_neg_limpio` por volcán), y también Chaitén 14/221, Llaima 0, Copahue 3. El script cuenta lo que su etiqueta dice. Los negativos limpios de los 6 volcanes de S135 (536) calzan con los del control reprocesado de S135 (535).
- **Las 12 noches de S135**: coinciden una a una con `resultado_final.json` (brazo D, `criterio1_detalle`). "Elimina el 100 %" es cierto también sobre el total: 0 de 217 records de nivel base falso en D.
- **Predicado**: sobre los artefactos de S135, el predicado node del dashboard y `publica_en_crater` discrepan en 1 record de 2.157 por brazo, y las 12 noches salen perdidas en D y publicadas en A y B con ambos.
- **Criterio 2 no es trivial ni inalcanzable**: en S135 el brazo D baja la publicación en negativos limpios de 387 a 178 pasadas (focal, n = 413) y de 115 a 52 (nevado, n = 122); un brazo sin compuerta puede subirla. Hay recorrido en las dos direcciones.
- **Perfiles**: los seis brazos leen lo que declara la tabla de §3 (`VRP_PROFILE=... python -c "import pipeline.profile"`), incluido `VRP_BG_NEIGHBOR_MAX_HALF_PX = 3`, conectiva `min` (`ENABLE_TESTS_23_PROSE_BRANCH=False`), corona apagada y sólo VIIRS 375. `tests/test_flags_d22_d25_perfil_s142.py`: 11 passed.
- **D25 llega a la magnitud del operador**: el bloque del Test 1 que reconstruye `anomaly_pixels` usa el fondo por vecinos (`process_viirs.py:1952-1965`) y `store.py:551-554` calcula `f5_core_vrp_mw` desde esos píxeles.
- **Costo de S135**: 48,8 y 53,1 horas de runner, jobs de 72 a 153 min (`gh run view --json jobs`). Los brazos nuevos no descargan MODIS ni VIIRS 750 (`scripts/run_pipeline.py:213-218`), así que ~160 h es una cota conservadora (SOSPECHA en cuánto).
- **Paper, renderizado con PyMuPDF a imagen y mirado** (`documentacion/sp426.5.pdf`, páginas del PDF):
  - p. 7: Tests 2 y 3 son `dNTI > C1 or dNTI > μ + C2σ` (ídem dETI), **sin condición de temperatura**. VERIFICADO.
  - p. 7, "Second run": se aplica "only if one or more pixels have been detected" y sobre "pixels adjacent to those already flagged". VERIFICADO.
  - p. 8, ec. 6: `L4bk` es "the arithmetic mean of all the pixels surrounding the active one (or around the active cluster)". VERIFICADO. No dice "no alertados" ni menciona recorte a cero.
  - "Siete textos más del grupo": SIN VERIFICAR (no leí esas páginas).
  - Que la figura A6 muestre el efecto que §1 atribuye a la compuerta: ver hallazgo 6, el propio catálogo lo corrigió.
