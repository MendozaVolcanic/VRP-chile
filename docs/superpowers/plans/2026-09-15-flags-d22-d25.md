# Flags D22 (compuerta de temperatura) y D25 (fondo por vecinos) en VIIRS 375: plan de implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Dejar escritos, en OFF en el perfil operacional y probados, dos flags de VIIRS 375: uno que quita la compuerta `bt > t_bg + 3 K` de los Tests 2 y 3 y del dNTI contextual (D22), y otro que calcula el fondo del VRP de cada píxel alertado como la media de la radiancia de sus vecinos no alertados (D25). Además, los perfiles de los brazos del A/B "literal completo + ablaciones".

**Architecture:** La compuerta se vuelve un parámetro explícito `apply_bt_gate=True` en los tres helpers compartidos de `pipeline/detection_context.py`. Sólo `process_viirs.py` lo pasa, y lo pasa en función del flag, así que MODIS y VIIRS 750 no cambian aunque el flag se encienda. El fondo por vecinos es un helper nuevo en `pipeline/vrp_regimes.py` más un envoltorio en `process_viirs.py` (mismo patrón que `apply_corona_magnitude_v375`, `process_viirs.py:619`). Se aplica **después** del cálculo de fondo de hoy, que queda como respaldo explícito. Un arnés sintético nuevo corre `calculate_vrp` de los tres procesadores con lectores falsos y congela la salida de hoy en un golden, contra el que se compara bit a bit.

**Tech Stack:** Python 3.12, NumPy 2.4.4 (medido), SciPy, PyYAML, pytest; perfiles YAML con `extends:` (`pipeline/profile.py:51-67`).

---

## 0. Por qué (el fenómeno antes que el código)

**La compuerta (D22).** De noche, en un cono nevado, la cumbre está más fría que el valle, porque la temperatura sigue a la altitud. Un lago de lava o un foco sub-píxel eleva mucho la radiancia en el infrarrojo medio (3,7 µm) y casi nada en el térmico (11 µm). Por eso el índice normalizado (NTI) y su contraste con los 8 vecinos (dNTI, dETI) lo delatan. Aun así, la temperatura de brillo de ese píxel puede quedar **por debajo** de la mediana del anillo regional de 5 a 25 km, que está lleno de valle tibio. La compuerta `bt > t_bg + 3 K` descarta justo ese píxel. El paper (Coppola 2016a, p. 7) no tiene esa condición en los Tests 2 y 3. Según el catálogo (`docs/MIROVA_DIVERGENCES.md:2257-2260`), la compuerta nació para el camino del NTI absoluto y después se heredó a los caminos contextuales.

**El fondo (D25).** Para calcular el VRP, a la radiancia del píxel caliente se le resta la radiancia del fondo. Hoy ese fondo es la mediana del mismo anillo regional de 5 a 25 km (`process_viirs.py:1413`), o bien un kernel 3x3 opt-in en 5 volcanes (`process_viirs.py:1401-1411`). En la cumbre helada, el cráter con lava sub-píxel está más frío que esa mediana: el exceso sale negativo y el recorte `np.maximum(..., 0.0)` (`process_viirs.py:1420`) lo deja en 0,0 MW, aunque la detección lo haya aceptado. MIROVA, según siete textos del grupo leídos en páginas renderizadas (`docs/MIROVA_DIVERGENCES.md:2299-2301`), resta en cambio la **media de la radiancia de los vecinos no alertados de cada píxel alertado**.

**Por qué van juntos y detrás de flags.** La corrección S138 (`docs/MIROVA_DIVERGENCES.md:2248`) midió que quitar sólo la compuerta no devuelve ninguna alerta. El segundo pase (`second_pass_adjacent`, `detection_context.py:803`) no recibe temperatura, corre aunque el primer pase no haya detectado nada (`ENABLE_SECOND_PASS_CONDITIONED = False`) y rescata el píxel que la compuerta rechazó. El cero lo pone el fondo. Lo reproduje en la escena sintética de este plan (probe S142, §2.3): con el perfil operacional, el primer pase deja 0 píxeles, el segundo rescata 2 y los dos se publican con 0,0 MW. Quitando la compuerta, el primer pase toma los 2, el segundo no agrega nada y la magnitud sigue en 0,0. Con fondo por vecinos, el cráter pasa a ~0,097 MW. **D22, D19/D2 y D25 no se pueden evaluar por separado**: el A/B es "literal completo + ablaciones" (spec `docs/superpowers/specs/2026-09-13-plan-definitivo-paridad-design.md` §4 Fase 1).

**Qué NO cambia este plan.** El recorte a cero del exceso negativo sigue igual (pregunta 4 del correo a Coppola). Tampoco cambian la conectiva `min` de los Tests 2 y 3 (`ENABLE_TESTS_23_PROSE_BRANCH`, S136, pregunta abierta), MODIS, VIIRS 750, el camino del NTI absoluto (Test 1 del paper, D23) ni el perfil operacional: los dos flags nacen en OFF (A45).

---

## 1. Evidencia verificada en esta sesión (base del plan)

Todo `archivo:línea` de esta sección salió de una lectura o de un comando corrido en la sesión que escribió el plan, sobre `main` en `f0020c7b2`. La diferencia con `55f2ac662` en `pipeline/`, `tests/`, `scripts/`, `CLAUDE.md` y los docs citados es vacía (`git diff --stat`). Lo no verificado lleva **SOSPECHA**.

### 1.1 Flags efectivos hoy (`VRP_PROFILE=mirova_equivalent python -c "import pipeline.profile as p; ..."`)

`NTI_BT_SANITY_K=3.0` · `ENABLE_LOCAL_KERNEL_BG=True` · `ENABLE_FIRST_PASS_TESTS_2_AND_3=True` · `ENABLE_SECOND_PASS_ADJACENT=True` · `ENABLE_SECOND_PASS_CONDITIONED=False` · `ENABLE_DNTI_CONTEXTUAL_PATH=True` · `ENABLE_DNTI_DUAL_ROI=True` · `ENABLE_ETI_QUADRATIC_SCENE=False` · `ENABLE_NTI_RELATIVE_PATH=False` · `ENABLE_TEST1_CONTEXTUAL_KEEP_PEAK=True` · `ENABLE_LOCAL_CLUSTER_MAGNITUDE_VIIRS375=False` · `ENABLE_TESTS_23_PROSE_BRANCH=False` · `ENABLE_TEST1_PATH=True` · `ENABLE_TEST1_CONTEXTUAL_FILTER=True` · `ENABLE_TEST1_SPATIAL_CORE=False` · `ENABLE_TEST1_PIXEL_FILTER=False` · `ENABLE_TEST1_LBG_GLOBAL=True` · `ENABLE_TEST1_INTERMEDIATE_BG=True` · `ENABLE_VENT_PATH=False` · `ENABLE_FINAL_PIXEL_FILTER=False` · `ENABLE_SINGLE_PIXEL_SUB_MW_MODE=True` · `ENABLE_UTM_REGRID=False` · `ENABLE_GEOLOCATED_PIXEL_AREA=False` · `ENABLE_TEST1_K1_RETIRE_FROM_HOT_MASK=False` · `PATH_D_ATM_GATE_TBG_MIN_K=None` · `CLOUD_MASK_BT_K=0.0` · `BG_INNER_KM, BG_OUTER_KM = 5.0, 25.0`.

### 1.2 Cómo lee el código cada flag (A89)

- `pipeline/profile.py:131` `_p = _cfg["paths"]`; `:79` `_t = _cfg["thresholds"]`. `ENABLE_SECOND_PASS_CONDITIONED` se lee de `_p` (`profile.py:403`) y el YAML lo declara bajo `paths:` (`mirova_equivalent.yaml:428`, con `paths:` en l. 138).
- Patrón de flag por sensor ya usado: `ENABLE_LOCAL_CLUSTER_MAGNITUDE_VIIRS375` (`profile.py:474-475`, `_p.get`) y `ENABLE_FOCAL_CLUSTER_MAGNITUDE_VIIRS750` (`profile.py:496-497`). Este plan sigue ese patrón: sufijo `_VIIRS375`, lectura desde `_p`.
- Parámetro numérico con validación al cargar: patrón `TEST1_INTERMEDIATE_BG_RING_KM` (`profile.py:369-375`, `_t.get` + `ValueError`).
- `process_viirs.py` importa las constantes del perfil por nombre (`process_viirs.py:89-189`), así que dentro del módulo son globales que `unittest.mock.patch.object(pv, NOMBRE, valor)` puede reemplazar. Lo comprobé con los probes (§2.3).
- El kernel local: `local_kernel_bg` (YAML de volcanes) llega como `local_kernel_bg_compatible` por el puente `scripts/run_pipeline.py:244` (también en 288 y 334). Está en `true` para PuyehueCordonCaulle (`volcanoes.yaml:54`), Villarrica (89), Chaiten (523), PlanchonPeteroa (623) y Lastarria (661).
- `run_pipeline.py:213-218` salta plataformas según `SENSOR_MODIS`, `SENSOR_VIIRS_375` y `SENSOR_VIIRS_750` del perfil. Por eso un brazo puede limitarse a V375.

### 1.3 Sitios de la compuerta (`grep -rn "bt_sanity_k\|NTI_BT_SANITY_K" pipeline/`)

| sitio | qué es | ¿lo apaga el flag D22? | por qué |
|---|---|---|---|
| `detection_context.py:269` (`contextual_dnti_hot_mask`) | dNTI contextual (camino D) | **sí, sólo cuando lo llama V375** | la fórmula del paper es dNTI contra C1; la compuerta no está |
| `detection_context.py:373`, `:376` (`dual_roi_contextual_dnti_hot_mask`) | reenvía `bt_sanity_k` al anterior | **sí, reenviando `apply_bt_gate`** | mismo test, umbral dual |
| `detection_context.py:532` (`first_pass_tests_2_and_3`) | Tests 2 ∧ 3 del primer pase | **sí, sólo cuando lo llama V375** | D22 literal (p. 7) |
| `process_viirs.py:1034-1043` y `:1045-1051` | llamadas V375 al dNTI contextual | **sí** (pasan `apply_bt_gate`) | la salida `dnti_ctx_hot` además alimenta el filtro contextual del Test 1 (`process_viirs.py:1788-1799`) |
| `process_viirs.py:1240-1261` | llamada V375 al primer pase | **sí** | en producción `hot_mask_2d = fp_hot` (`:1262`) |
| `process_viirs.py:1180-1181` | camino ETI cuadrático: Tests 2/3 vía `second_pass_adjacent` y después la compuerta | **sí** | son los Tests 2/3 del paper. **Inerte hoy** (`ENABLE_ETI_QUADRATIC_SCENE=False`) |
| `process_viirs.py:979` | camino B: `nti > NTI_K1_NIGHT` y compuerta (Test 1 del paper) | **no** | es D23, no D22. Su conteo `n_nti_path` decide `only_test1_source` (`:1696-1702`) y el tope D9 (`:1370-1377`); tocarlo cambia la selección de fuente. **SOSPECHA**: que el Test 1 del paper tampoco tenga condición de temperatura (no leí esa página en esta sesión) |
| `process_viirs.py:1001` | camino C, NTI relativo (S11, no es del paper) | **no** | no es fórmula del paper y está OFF |
| `process_modis.py:667, 695, 703, 823, 871` | los mismos cinco usos en MODIS | **no** | no pasan `apply_bt_gate` → default `True` |
| `process_viirs_mod.py:626, 645, 679, 687, 782, 834` | los mismos seis usos en VIIRS 750 | **no** | ídem |
| `profile.py:88` | definición `NTI_BT_SANITY_K` | no se toca | |
| `detection_context.py:803-948` (`second_pass_adjacent`) | segundo pase | **no tiene compuerta**: su firma (`:803-820`) no recibe `bt` | es la corrección S138; D2/D19 van por `ENABLE_SECOND_PASS_CONDITIONED` |

### 1.4 Dónde se resta un fondo a la radiancia de píxeles publicados en V375

| sitio | qué publica | ¿entra el fondo nuevo? |
|---|---|---|
| `process_viirs.py:1387-1424` (`L_bg`, `delta_L`, `per_pixel_vrp_mw`) | `vrp_mir_mw` (`:1424`), `anomaly_pixels[*].vrp_mw` (`:1436-1445`), el VRP del cúmulo (`:1455-1476`) y `apply_single_pixel_mode` (`:1519-1527`) | **sí** |
| `store.py:551-554` (`f5_core_vrp_mw`) | lo que el dashboard publica en V375; suma `anomaly_pixels[*].vrp_mw` (`f5_core.py:97-104`) | **sí, por herencia**: `store.py` no resta fondo (grep de `t_bg|L_bg|bt_to_spectral|delta_L` en `store.py`: sólo un ejemplo de docstring en l. 31) |
| `process_viirs.py:1861-1873` (Test 1, `vrp_mir_mw`) | `vrp_mir_mw` cuando `final_hotspot_source == "test1"` | **sí** |
| `process_viirs.py:1881-1902` (Test 1, cúmulo y `anomaly_pixels`) | reconstruye `anomaly_pixels` (`:1902`) que lee F5 | **sí** |
| `process_viirs.py:1847-1859` (núcleo espacial del Test 1) | sólo **elige** píxeles por energía | **no**: `ENABLE_TEST1_SPATIAL_CORE=False` y es selección, no magnitud publicada |
| `process_viirs.py:1491-1497` (corona Eq.6 `apply_corona_magnitude_v375`) | pisa `pc.vrp_mw` si su flag está ON | **no**: `ENABLE_LOCAL_CLUSTER_MAGNITUDE_VIIRS375=False`; los brazos lo mantienen OFF y un test lo verifica |
| `process_viirs.py:1648-1653` (camino vent) | `vrp_vent_mw` | **no**: `ENABLE_VENT_PATH=False` |
| `test1_integrated.py:412` (`L_bg = np.median(...)` del Test 1 integrado) | estadístico de **detección** del Test 1 | **no**: es detección (D23), no el fondo del VRP |
| `process_viirs.py:1544-1578` (VRP TIR) | `vrp_tir_mw` | **no**: TIR y `ENABLE_VRP_TIR_OUTPUT` del perfil operacional apagado |

### 1.5 Guards que el cambio no puede romper

- `tests/test_fondo_persistido_s140.py:65-74` exige, en `process_viirs.py`, que `diag_L_bg_local = None` aparezca antes de `if ENABLE_LOCAL_KERNEL_BG and local_kernel_bg_compatible:` y éste antes de `diag_L_bg_local = redondear_diag(float(np.nanmedian(L_bg)))`, y toma la **primera** coincidencia. El código nuevo va **después** de ese bloque y usa otro nombre de variable (`diag_L_bg_vecinos`).
- `tests/test_guard_declarado_vs_efectivo_s131.py:146-158` (contrato G8) fija `process_viirs.py:83` (`FLAG_DNS`) y `process_viirs.py:212` (`compute_test1_nti`). Los imports nuevos van **después** de la l. 215 para no mover esas dos líneas.
- `CLAUDE.md:813` cita `process_viirs.py:212/1082`. El 1082 (`test1_res = compute_test1_nti(`) **se va a correr** con este cambio. Tarea 9 lo corrige.
- `experiments/_s133/auditar_guards_por_subcadena.py` (A92) hoy da "candidatos a falso verde: 0". **Ojo**: el script reescribe `experiments/_s133/auditar_guards_por_subcadena.json`, que está versionado. Restaurarlo después de correrlo (`git checkout -- experiments/_s133/auditar_guards_por_subcadena.json`) salvo que se quiera commitear el cambio.

---

## 2. Mapa de archivos

### 2.1 Crear

| archivo | responsabilidad |
|---|---|
| `tests/arnes_sintetico_s142.py` | escenas sintéticas y ejecución de `calculate_vrp` de V375, V750 y MODIS con lectores falsos; JSON canónico; CLI con `--parche` y `--solo` |
| `tests/golden_s142/apagado.json` | salida de hoy (antes de tocar código) bajo `mirova_equivalent` |
| `tests/test_apagado_no_cambia_nada_s142.py` | (a) bit a bit contra el golden; (d) MODIS y V750 con los dos flags ON por perfil |
| `tests/test_d22_compuerta_bt_s142.py` | (b) de D22: helpers y extremo a extremo; guards de fuente |
| `tests/test_d25_fondo_vecinos_s142.py` | (b) de D25: helper, envoltorio y extremo a extremo; guards de fuente |
| `tests/test_flags_d22_d25_perfil_s142.py` | (c) perfil operacional en False, sección correcta del YAML, cada brazo lee lo que declara |
| `pipeline/profiles/_s142_verif_flags_nuevos.yaml` | instrumento del test (d): sólo los dos flags nuevos ON (no es brazo) |
| `pipeline/profiles/_s142_ab_control.yaml`, `_s142_ab_literal.yaml`, `_s142_ab_lit_sin_fondo.yaml`, `_s142_ab_lit_con_compuerta.yaml`, `_s142_ab_lit_sp_suelto.yaml`, `_s142_ab_lit_keep_peak.yaml` | brazos del A/B, V375 sólo, `data_subdir` aislado |

### 2.2 Modificar

| archivo | dónde (verificado) | qué |
|---|---|---|
| `pipeline/detection_context.py` | firma `:208-221` y cuerpo `:264-270`; firma `:316-333` y `_kwargs` `:365-371`; firma `:399-423` y cuerpo `:527-533` | parámetro `apply_bt_gate: bool = True` |
| `pipeline/vrp_regimes.py` | después de `compute_local_background` (termina en `:106`), antes de `cluster_corona_background` (`:109`) | helper `neighbor_mean_radiance_background` |
| `pipeline/profile.py` | después de `:475` | `ENABLE_TESTS_23_NO_BT_GATE_VIIRS375`, `ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375`, `VRP_BG_NEIGHBOR_MAX_HALF_PX` |
| `pipeline/profiles/mirova_equivalent.yaml` | `thresholds:` después de `:30`; `paths:` después de `:428` y de `:442` | los tres valores, flags en `false`, con comentario POR QUÉ |
| `pipeline/process_viirs.py` | imports después de `:215`; envoltorio después de `:671`; inicialización después de `:897`; `:1041`, `:1049`, `:1180-1181`, `:1260`; después de `:1413`; `:1868-1869`; `:1892-1893`; antes de `:2131` | cableado de los dos flags |
| `tests/test_gr2_profile_invariants.py` | `EXPECTED_OPERATIONAL_FLAGS` `:91-133`; `const_to_yaml` `:152-163` | fijar los dos flags en False y su clave YAML |
| `docs/MIROVA_DIVERGENCES.md` | entradas `## D22` (`:2246`) y `## D25` (`:2295`) | nota S142: flag, alcance y estado OFF |
| `docs/FICHA_SDA_VRP_CHILE.md` | tabla de versiones (`:46-54`) | fila v1.6 |
| `CLAUDE.md` | `:813` | corregir la cita `process_viirs.py:212/1082` (requiere visto bueno de Nicolás: es el archivo de reglas) |

---

## 3. Tareas

### Tarea 0: rama, tag defensivo y línea base (antes del primer edit)

**Files:** ninguno.

- [ ] **Paso 1: partir de `main` al día**

```bash
cd "C:/Users/nmend/OneDrive/Escritorio/claude/Volcanologia/VRP Chile"
git fetch origin --prune
git status --short            # sólo debe aparecer "?? experiments/_s140/"
git stash list                # A96: mirar antes de cualquier stash; no hacer pop
git checkout -b s142-flags-d22-d25 origin/main
```

- [ ] **Paso 2: tag defensivo (A45) y push**

```bash
git tag -a pre-s142-flags-d22-d25 -m "Snapshot defensivo antes de tocar pipeline/detection_context.py, pipeline/vrp_regimes.py, pipeline/process_viirs.py, pipeline/profile.py y pipeline/profiles/mirova_equivalent.yaml (A45). Flags D22 y D25 de VIIRS 375, en OFF." origin/main
git push origin pre-s142-flags-d22-d25
git cat-file -t pre-s142-flags-d22-d25      # esperado: tag
```

- [ ] **Paso 3: confirmación explícita de Nicolás (A45)**

Pedirle a Nicolás un "sí" explícito para tocar los cinco archivos de `pipeline/` listados, aunque todo nazca en OFF. Sin ese sí, no pasar a la Tarea 1.

- [ ] **Paso 4: línea base de la suite**

```bash
python -m pytest -q -p no:cacheprovider 2>&1 | tail -3
```

Anotar el conteo exacto (hoy se recolectan 1451 tests, medido con `--co`; los passed/skipped/failed **no** se midieron al escribir el plan). Todo fallo que aparezca en la línea base se registra aquí y no se atribuye después a este cambio (A50: verificar contra `origin/main`).

---

### Tarea 1: arnés sintético y golden de hoy (sin tocar `pipeline/`)

**Files:**
- Create: `tests/arnes_sintetico_s142.py`
- Create: `tests/golden_s142/apagado.json` (generado)

- [ ] **Paso 1: escribir el arnés**

```python
# -*- coding: utf-8 -*-
"""Arnés sintético S142 (D22/D25): corre `calculate_vrp` de los tres procesadores sobre escenas
fabricadas, con los lectores de gránulo reemplazados, y vuelca el resultado a JSON canónico.

POR QUÉ EXISTE. Hasta S140 no había un test que procesara un gránulo de punta a punta
(tests/test_fondo_persistido_s140.py lo dice en su docstring), así que "apagado no cambia nada"
sólo se podía afirmar leyendo código. Este arnés congela la salida de hoy en
tests/golden_s142/apagado.json y la compara, como TEXTO, con la de después. Se compara texto y no
diccionarios porque NaN != NaN en Python (el mismo modo de falla que S132).

LA ESCENA PRINCIPAL. Un cono nevado de noche: cumbre a 255 K y valle más tibio (+1,2 K por km
hasta 12 km). En el cráter hay un foco sub-píxel (el MIR sube a 266 K y el TIR casi no) y a su
lado un vecino tibio (262 K). Los dos quedan bajo la compuerta t_bg + 3 K (t_bg ~269,4 K) y bajo
la mediana del anillo, que es exactamente lo que miden D22 y D25.

Uso:
    VRP_PROFILE=mirova_equivalent python tests/arnes_sintetico_s142.py --salida X.json \
        [--parche NOMBRE=valor ...] [--solo v375]
Los parches reemplazan constantes de módulo de pipeline.process_viirs. Un nombre que no existe
hace fallar la corrida (patch.object levanta AttributeError): un brazo que no lee su flag no
puede pasar por control (S133).
"""
import argparse
import ast
import json
import os
import sys
from pathlib import Path
from unittest.mock import patch

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))
os.environ.setdefault("VRP_PROFILE", "mirova_equivalent")

import numpy as np  # noqa: E402

LAT0, LON0 = -39.42, -71.94
N_V375 = 161
CENTRO_V375 = N_V375 // 2
_ANGULOS = {"sensor_zenith_deg": None, "sensor_azimuth_deg": None,
            "solar_zenith_deg": None, "solar_azimuth_deg": None}
ESCENAS_V375 = ("nevado_vecino_tibio", "foco_3x3", "plana")
ESCENAS_GRUESAS = ("nevado", "plana")


def _malla(n, px_km):
    d = (np.arange(n) - n // 2) * px_km
    y, x = np.meshgrid(d, d, indexing="ij")
    lat = (LAT0 + y / 111.0).astype(np.float32)
    lon = (LON0 + x / (111.0 * np.cos(np.radians(LAT0)))).astype(np.float32)
    return lat, lon, np.hypot(y, x)


def _geo(lat, lon):
    return {"lat": lat, "lon": lon, "sensor_zenith": np.zeros(lat.shape, np.float32),
            "angles": dict(_ANGULOS)}


def _cono(r, rng):
    return 255.0 + np.clip(r, 0, 12) * 1.2 + rng.normal(0, 0.15, r.shape)


def escena_v375(tipo, semilla=0):
    lat, lon, r = _malla(N_V375, 0.375)
    rng = np.random.default_rng(semilla)
    bt4 = _cono(r, rng)
    bt5 = bt4 - 1.0 + rng.normal(0, 0.15, r.shape)
    c = CENTRO_V375
    if tipo == "nevado_vecino_tibio":
        bt4[c, c], bt5[c, c] = 266.0, 255.5
        bt4[c, c + 1], bt5[c, c + 1] = 262.0, 254.5
    elif tipo == "foco_3x3":
        bt4[c - 1:c + 2, c - 1:c + 2] = 300.0
        bt4[c, c] = 320.0
        bt5[c - 1:c + 2, c - 1:c + 2] += 3.0
    elif tipo != "plana":
        raise ValueError(tipo)
    return {"I04": bt4.astype(np.float32), "I05": bt5.astype(np.float32)}, _geo(lat, lon)


def escena_v750(tipo, semilla=1):
    n = 81
    lat, lon, r = _malla(n, 0.75)
    rng = np.random.default_rng(semilla)
    m13 = _cono(r, rng)
    m15 = m13 - 1.0 + rng.normal(0, 0.15, r.shape)
    c = n // 2
    if tipo == "nevado":
        m13[c, c], m15[c, c] = 275.0, 256.0
        m13[c, c + 1], m15[c, c + 1] = 263.0, 255.0
    elif tipo != "plana":
        raise ValueError(tipo)
    return {"M13": m13.astype(np.float32), "M15": m15.astype(np.float32)}, _geo(lat, lon)


def escena_modis(tipo, semilla=2):
    import pipeline.process_modis as pm
    n = 61
    lat, lon, r = _malla(n, 1.0)
    rng = np.random.default_rng(semilla)
    bt21 = _cono(r, rng)
    bt31 = bt21 - 1.0 + rng.normal(0, 0.15, r.shape)
    c = n // 2
    if tipo == "nevado":
        bt21[c, c], bt31[c, c] = 290.0, 258.0
    elif tipo != "plana":
        raise ValueError(tipo)

    def rad(bt, lam):
        return (pm.C1 / (lam ** 5 * (np.exp(pm.C2 / (lam * bt)) - 1))).astype(np.float32)

    lam21 = pm.BAND21_LAMBDA
    return {"band21": rad(bt21, lam21), "band22": rad(bt21, lam21),
            "band31": rad(bt31, 11.03), "lat": lat, "lon": lon}


def _con_parches(modulo, parches):
    ctx = [patch.object(modulo, k, v) for k, v in parches.items()]
    for p in ctx:
        p.start()
    return ctx


def correr_v375(tipo, local_kernel_bg_compatible, parches):
    import pipeline.process_viirs as pv
    bands, geo = escena_v375(tipo)
    ctx = _con_parches(pv, parches)
    try:
        with patch.object(pv, "read_viirs_l1b", return_value=bands), \
                patch.object(pv, "read_viirs_geo", return_value=geo):
            return pv.calculate_vrp(
                Path("VNP02IMG.A2026200.0600.002.nc"), Path("VNP03IMG.sintetico.nc"),
                volcano_lat=LAT0, volcano_lon=LON0, radius_km=25.0,
                vent_lat=LAT0, vent_lon=LON0, inner_radius_km=5.0,
                local_kernel_bg_compatible=local_kernel_bg_compatible)
    finally:
        for p in ctx:
            p.stop()


def correr_v750(tipo):
    import pipeline.process_viirs_mod as pvm
    bands, geo = escena_v750(tipo)
    with patch.object(pvm, "read_viirs_mod_l1b", return_value=bands), \
            patch.object(pvm, "read_viirs_mod_geo", return_value=geo):
        return pvm.calculate_vrp(
            Path("VNP02MOD.A2026200.0600.002.nc"), Path("VNP03MOD.sintetico.nc"),
            volcano_lat=LAT0, volcano_lon=LON0, radius_km=25.0,
            vent_lat=LAT0, vent_lon=LON0, inner_radius_km=5.0)


def correr_modis(tipo):
    import pipeline.process_modis as pm
    data = escena_modis(tipo)
    with patch.object(pm, "read_modis_l1b", return_value=data):
        return pm.calculate_vrp(
            hdf_path=Path("MOD021KM.A2026200.0300.061.hdf"), geo_path=Path("MOD03.sintetico.hdf"),
            volcano_lat=LAT0, volcano_lon=LON0, radius_km=25.0,
            vent_lat=LAT0, vent_lon=LON0, inner_radius_km=5.0)


def entradas_helpers():
    """Entradas de los helpers de detection_context sacadas de la escena principal."""
    import pipeline.process_viirs as pv
    from pipeline.detection_context import compute_nti_and_nti_app
    bands, geo = escena_v375("nevado_vecino_tibio")
    bt4 = bands["I04"].astype(np.float64)
    bt5 = bands["I05"].astype(np.float64)
    l_mir = pv.bt_to_spectral_radiance(bt4, pv.I04_LAMBDA)
    nti, nti_app = compute_nti_and_nti_app(rad_mir=l_mir, bt_tir=bt5,
                                           lambda_mir_um=pv.I04_LAMBDA, lambda_tir_um=11.450)
    dist = pv.haversine_km(LAT0, LON0, geo["lat"], geo["lon"])
    roi = pv.roi_mask_bbox(geo["lat"], geo["lon"], LAT0, LON0, 25.0)
    t_bg = float(np.median(bt4[(dist >= 5.0) & (dist <= 25.0)]))
    return {"bt": bt4, "nti": nti, "nti_app": nti_app, "dist": dist, "roi": roi, "t_bg": t_bg}


def salidas_helpers(**extra):
    """Las tres funciones con la compuerta, llamadas como las llama producción.
    `extra` se reenvía a las tres (lo usa el test de D22 con apply_bt_gate=False)."""
    from pipeline.detection_context import (contextual_dnti_hot_mask,
                                            dual_roi_contextual_dnti_hot_mask,
                                            first_pass_tests_2_and_3)
    e = entradas_helpers()
    ctx = contextual_dnti_hot_mask(nti=e["nti"], bt=e["bt"], roi_mask=e["roi"], t_bg=e["t_bg"],
                                   c1=0.003, bt_sanity_k=3.0, apply_unsuitable_filters=True, **extra)
    dual = dual_roi_contextual_dnti_hot_mask(nti=e["nti"], bt=e["bt"], roi_mask=e["roi"],
                                             dist_km=e["dist"], t_bg=e["t_bg"], c1_summit=0.003,
                                             c1_scene=0.010, inner_km=5.0, bt_sanity_k=3.0,
                                             apply_unsuitable_filters=True, **extra)
    fp, diag = first_pass_tests_2_and_3(nti=e["nti"], nti_app=e["nti_app"], bt=e["bt"],
                                        roi_mask=e["roi"], dist_km=e["dist"], t_bg=e["t_bg"],
                                        bt_sanity_k=3.0, inner_km=5.0, c1_dnti_scene=0.010,
                                        c1_deti_scene=0.010, c2_dnti_scene=10, c2_deti_scene=10,
                                        **extra)
    return {
        "contextual": np.flatnonzero(ctx).tolist(),
        "dual": np.flatnonzero(dual).tolist(),
        "first_pass": np.flatnonzero(fp).tolist(),
        "first_pass_diag": {k: diag[k] for k in ("n_first_pass_pixels", "mu_dnti", "sd_dnti",
                                                  "mu_deti", "sd_deti", "n_bg_used")},
    }


def generar(parches, solo=None):
    import pipeline.profile as prof
    out = {"perfil": prof.PROFILE_NAME}
    if solo in (None, "helpers"):
        out["helpers"] = salidas_helpers()
    if solo in (None, "v375"):
        out["v375"] = {f"{t}|kernel={k}": correr_v375(t, k, parches)
                       for t in ESCENAS_V375 for k in (False, True)}
    if solo in (None, "v750"):
        out["v750"] = {t: correr_v750(t) for t in ESCENAS_GRUESAS}
    if solo in (None, "modis"):
        out["modis"] = {t: correr_modis(t) for t in ESCENAS_GRUESAS}
    return out


def _a_json(o):
    if isinstance(o, np.generic):
        return o.item()
    if isinstance(o, np.ndarray):
        return o.tolist()
    return repr(o)


def canonico(obj):
    return json.dumps(obj, sort_keys=True, indent=1, default=_a_json, ensure_ascii=False)


def correr_en_subproceso(tmp_dir, perfil="mirova_equivalent", parches=(), solo=None):
    """Corre el arnés en un proceso limpio (pipeline.profile se lee al importar) y devuelve el
    TEXTO del JSON. Cada perfil o parche necesita su propio proceso."""
    import subprocess
    salida = Path(tmp_dir) / f"arnes_{perfil}_{len(parches)}_{solo}.json"
    cmd = [sys.executable, str(ROOT / "tests" / "arnes_sintetico_s142.py"), "--salida", str(salida)]
    for p in parches:
        cmd += ["--parche", p]
    if solo:
        cmd += ["--solo", solo]
    r = subprocess.run(cmd, cwd=str(ROOT), capture_output=True, text=True, timeout=1800,
                       env={**os.environ, "VRP_PROFILE": perfil, "PYTHONIOENCODING": "utf-8"})
    assert r.returncode == 0, r.stderr[-4000:]
    return salida.read_text(encoding="utf-8")


def main(argv=None):
    ap = argparse.ArgumentParser()
    ap.add_argument("--salida", required=True)
    ap.add_argument("--parche", action="append", default=[])
    ap.add_argument("--solo", choices=("helpers", "v375", "v750", "modis"), default=None)
    a = ap.parse_args(argv)
    parches = {}
    for s in a.parche:
        k, v = s.split("=", 1)
        parches[k] = ast.literal_eval(v)
    texto = canonico(generar(parches, a.solo))
    Path(a.salida).parent.mkdir(parents=True, exist_ok=True)
    Path(a.salida).write_text(texto, encoding="utf-8")


if __name__ == "__main__":
    main()
```

- [ ] **Paso 2: generar el golden con el código de hoy**

```bash
VRP_PROFILE=mirova_equivalent PYTHONIOENCODING=utf-8 python tests/arnes_sintetico_s142.py --salida tests/golden_s142/apagado.json
python -c "import json; d=json.load(open('tests/golden_s142/apagado.json',encoding='utf-8')); r=d['v375']['nevado_vecino_tibio|kernel=False']; print(d['perfil'], r['diag_n_first_pass_pixels'], r['diag_n_second_pass_recapture'], [(p['bt_k'],p['vrp_mw']) for p in r['anomaly_pixels']])"
```

Salida esperada del segundo comando (medida con el probe S142 del escritor, idéntica escena):
`mirova_equivalent 0 2 [(266.0, 0.0), (262.0, 0.0)]`.

Si no coincide, **parar**: la escena no ejerce lo que dice, y los tests de las Tareas 3 y 5 no significarían nada.

- [ ] **Paso 3: comprobar que el arnés es determinista**

```bash
VRP_PROFILE=mirova_equivalent PYTHONIOENCODING=utf-8 python tests/arnes_sintetico_s142.py --salida "$TMP/arnes_bis.json"
cmp tests/golden_s142/apagado.json "$TMP/arnes_bis.json" && echo IDENTICO
```

Esperado: `IDENTICO`. El probe S142 midió dos corridas V375 idénticas en `json.dumps(sort_keys=True)`.

- [ ] **Paso 4: commit**

```bash
git add tests/arnes_sintetico_s142.py tests/golden_s142/apagado.json
git commit -m "S142: arnés sintético de calculate_vrp (V375, V750, MODIS) y golden del código de hoy

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tarea 2: test "apagado no cambia nada" (rojo hasta que exista el perfil de verificación)

**Files:**
- Create: `tests/test_apagado_no_cambia_nada_s142.py`

- [ ] **Paso 1: escribir el test**

```python
# -*- coding: utf-8 -*-
"""S142: con los flags D22 y D25 en OFF, la salida es BIT A BIT la de antes del cambio; con los dos
encendidos por perfil, MODIS y VIIRS 750 no se mueven.

POR QUÉ. El cron NRT corre 12 veces al día sobre 11 volcanes: un cambio que pase los tests
unitarios pero mueva un número se replica a cientos de records antes de verse (A45). El golden se
generó con el código previo (Tarea 1) y se compara como texto (NaN != NaN, S132).
"""
import json
import os
import sys
from pathlib import Path

import pytest

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT / "tests"))

from arnes_sintetico_s142 import correr_en_subproceso  # noqa: E402

GOLDEN = ROOT / "tests" / "golden_s142" / "apagado.json"
PERFIL_VERIF = "_s142_verif_flags_nuevos"


def test_perfil_operacional_reproduce_el_golden_bit_a_bit(tmp_path):
    texto = correr_en_subproceso(tmp_path, perfil="mirova_equivalent")
    assert texto == GOLDEN.read_text(encoding="utf-8"), (
        "la salida con el perfil operacional cambió respecto del código previo: los flags "
        "D22/D25 en OFF NO pueden cambiar nada")


@pytest.fixture(scope="module")
def salida_verif(tmp_path_factory):
    if not (ROOT / "pipeline" / "profiles" / f"{PERFIL_VERIF}.yaml").exists():
        pytest.fail(f"falta el perfil {PERFIL_VERIF} (Tarea 6)")
    return json.loads(correr_en_subproceso(tmp_path_factory.mktemp("verif"), perfil=PERFIL_VERIF))


def test_modis_y_viirs750_no_cambian_con_los_dos_flags_on(salida_verif):
    golden = json.loads(GOLDEN.read_text(encoding="utf-8"))
    for sensor in ("modis", "v750"):
        assert json.dumps(salida_verif[sensor], sort_keys=True) == json.dumps(golden[sensor], sort_keys=True), sensor


def test_los_helpers_con_su_default_no_cambian_con_los_flags_on(salida_verif):
    """Los helpers compartidos se llaman sin apply_bt_gate: el default debe seguir siendo la compuerta."""
    golden = json.loads(GOLDEN.read_text(encoding="utf-8"))
    assert salida_verif["helpers"] == golden["helpers"]


def test_el_perfil_de_verificacion_si_mueve_viirs375(salida_verif):
    """Control de instrumento (S128): si V375 tampoco cambiara, el perfil no estaría leyendo sus flags
    y el test anterior pasaría por la razón equivocada."""
    golden = json.loads(GOLDEN.read_text(encoding="utf-8"))
    r_on = salida_verif["v375"]["nevado_vecino_tibio|kernel=False"]
    r_off = golden["v375"]["nevado_vecino_tibio|kernel=False"]
    assert r_on["diag_n_first_pass_pixels"] != r_off["diag_n_first_pass_pixels"]
    assert "diag_bg_vecinos_n_sin_vecinos" in r_on and "diag_bg_vecinos_n_sin_vecinos" not in r_off
```

- [ ] **Paso 2: correrlo**

```bash
python -m pytest tests/test_apagado_no_cambia_nada_s142.py -v
```

Esperado: `test_perfil_operacional_reproduce_el_golden_bit_a_bit` PASS (todavía no se tocó nada). Los otros tres FAIL con `falta el perfil _s142_verif_flags_nuevos (Tarea 6)`. Este test queda en rojo a propósito hasta la Tarea 6.

- [ ] **Paso 3: commit**

```bash
git add tests/test_apagado_no_cambia_nada_s142.py
git commit -m "S142: test apagado-no-cambia-nada contra el golden y MODIS/V750 invariantes

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tarea 3: D22 en los helpers de `detection_context.py`

**Files:**
- Create: `tests/test_d22_compuerta_bt_s142.py`
- Modify: `pipeline/detection_context.py:208-270`, `:316-378`, `:399-533`

- [ ] **Paso 1: escribir los tests de helpers (rojos)**

```python
# -*- coding: utf-8 -*-
"""S142 D22: la compuerta `bt > t_bg + bt_sanity_k` dentro del dNTI contextual y de los Tests 2 y 3.

EL FENÓMENO. En un cono nevado, de noche, el píxel con lava sub-píxel puede estar más frío que la
mediana del anillo regional (lleno de valle tibio) y aun así destacar en el índice espectral
contra sus 8 vecinos. La fórmula de los Tests 2 y 3 de Coppola 2016a (p. 7) no tiene condición de
temperatura; la compuerta la heredamos del camino del NTI absoluto (docs/MIROVA_DIVERGENCES.md D22).

QUÉ FIJAN. (1) El default de los tres helpers es la compuerta de hoy. (2) `apply_bt_gate=False`
deja entrar exactamente al píxel que la compuerta rechazaba. (3) Sin compuerta sigue exigiendo BT
finita. (4) El segundo pase no tiene compuerta (corrección S138). (5) Sólo VIIRS 375 la apaga.
"""
import inspect
import json
import re
import sys
from pathlib import Path

import numpy as np
import pytest

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))
sys.path.insert(0, str(ROOT / "tests"))

from arnes_sintetico_s142 import (CENTRO_V375, N_V375, correr_en_subproceso,  # noqa: E402
                                  entradas_helpers, salidas_helpers)
from pipeline.detection_context import (contextual_dnti_hot_mask,  # noqa: E402
                                        dual_roi_contextual_dnti_hot_mask,
                                        first_pass_tests_2_and_3, second_pass_adjacent)

GOLDEN = json.loads((ROOT / "tests" / "golden_s142" / "apagado.json").read_text(encoding="utf-8"))
CRATER = CENTRO_V375 * N_V375 + CENTRO_V375
VECINO = CRATER + 1


def _src(rel):
    return (ROOT / rel).read_text(encoding="utf-8")


def _tokens(src, token):
    return len(re.findall(r"(?<![A-Za-z0-9_])" + re.escape(token) + r"(?![A-Za-z0-9_])", src))


def test_default_y_explicito_true_son_la_compuerta_de_hoy():
    assert salidas_helpers() == GOLDEN["helpers"]
    assert salidas_helpers(apply_bt_gate=True) == GOLDEN["helpers"]


def test_sin_compuerta_el_primer_pase_toma_al_crater_y_al_vecino_tibio():
    hoy = GOLDEN["helpers"]
    sin = salidas_helpers(apply_bt_gate=False)
    assert CRATER not in hoy["first_pass"] and VECINO not in hoy["first_pass"]
    assert CRATER in sin["first_pass"] and VECINO in sin["first_pass"]
    # sólo cambia el conjunto de activos: mu y sigma salen del pool de fondo, que no depende de BT
    for k in ("mu_dnti", "sd_dnti", "mu_deti", "sd_deti", "n_bg_used"):
        assert sin["first_pass_diag"][k] == hoy["first_pass_diag"][k], k
    # y no aparece ningún píxel que no estuviera ya en la escena con la compuerta bajada a -inf
    e = entradas_helpers()
    ref, _ = first_pass_tests_2_and_3(nti=e["nti"], nti_app=e["nti_app"], bt=e["bt"],
                                      roi_mask=e["roi"], dist_km=e["dist"], t_bg=e["t_bg"],
                                      bt_sanity_k=-np.inf, inner_km=5.0, c1_dnti_scene=0.010,
                                      c1_deti_scene=0.010, c2_dnti_scene=10, c2_deti_scene=10)
    assert sin["first_pass"] == np.flatnonzero(ref).tolist()


def _escena_minima():
    """7x7: NTI plano a -0,90 y el centro a -0,85 (dNTI = 0,05 > C1). BT del centro 260 K contra
    t_bg 270 K: la compuerta (273 K) lo rechaza."""
    nti = np.full((7, 7), -0.90)
    nti[3, 3] = -0.85
    bt = np.full((7, 7), 255.0)
    bt[3, 3] = 260.0
    roi = np.ones((7, 7), dtype=bool)
    return nti, bt, roi


def test_dnti_contextual_sin_compuerta_toma_el_centro():
    nti, bt, roi = _escena_minima()
    con = contextual_dnti_hot_mask(nti, bt, roi, 270.0, 0.003, 3.0)
    sin = contextual_dnti_hot_mask(nti, bt, roi, 270.0, 0.003, 3.0, apply_bt_gate=False)
    assert not con.any()
    assert sin[3, 3] and int(sin.sum()) == 1


def test_dual_roi_reenvia_la_compuerta():
    nti, bt, roi = _escena_minima()
    dist = np.zeros((7, 7))
    con = dual_roi_contextual_dnti_hot_mask(nti, bt, roi, dist, 270.0, 0.003, 0.010, 5.0, 3.0)
    sin = dual_roi_contextual_dnti_hot_mask(nti, bt, roi, dist, 270.0, 0.003, 0.010, 5.0, 3.0,
                                            apply_bt_gate=False)
    assert not con.any()
    assert sin[3, 3] and int(sin.sum()) == 1


def test_sin_compuerta_sigue_exigiendo_bt_finita():
    nti, bt, roi = _escena_minima()
    bt[3, 3] = np.nan
    sin = contextual_dnti_hot_mask(nti, bt, roi, 270.0, 0.003, 3.0, apply_bt_gate=False)
    assert not sin[3, 3]


def test_el_segundo_pase_no_recibe_temperatura():
    """Corrección S138: el segundo pase no tiene compuerta; por eso rescata lo que ella rechaza."""
    assert "bt" not in inspect.signature(second_pass_adjacent).parameters
    assert "apply_bt_gate" not in inspect.signature(second_pass_adjacent).parameters
```

- [ ] **Paso 2: correr y ver el rojo**

```bash
python -m pytest tests/test_d22_compuerta_bt_s142.py -v
```

Esperado: `test_default_y_explicito_true_son_la_compuerta_de_hoy`, `test_sin_compuerta_el_primer_pase_toma_al_crater_y_al_vecino_tibio`, `test_dnti_contextual_sin_compuerta_toma_el_centro`, `test_dual_roi_reenvia_la_compuerta` y `test_sin_compuerta_sigue_exigiendo_bt_finita` FAIL con `TypeError: ... got an unexpected keyword argument 'apply_bt_gate'`. `test_el_segundo_pase_no_recibe_temperatura` PASS.

- [ ] **Paso 3: implementar en `contextual_dnti_hot_mask`**

En la firma (`detection_context.py:220`), después de `deti: Optional[np.ndarray] = None,` agregar:

```python
    apply_bt_gate: bool = True,
```

Reemplazar el bloque `detection_context.py:264-270`:

```python
    hot = (
        roi_mask
        & ~np.isnan(dnti)
        & ~np.isnan(bt)
        & (dnti > c1)
        & (bt > t_bg + bt_sanity_k)
    )
```

por:

```python
    # D22 (S142): la compuerta de temperatura no está en la fórmula del paper (Coppola 2016a
    # p. 7, Tests 2 y 3: dNTI contra C1 o mu + C2 sigma). Nació en el camino del NTI absoluto
    # (commit 59846e897) y se heredó acá. En un cono nevado de noche descarta al píxel con lava
    # sub-píxel que está más frío que la mediana del anillo regional. `apply_bt_gate=False` la
    # quita; el default conserva el comportamiento histórico. Sólo process_viirs.py la apaga,
    # con ENABLE_TESTS_23_NO_BT_GATE_VIIRS375. La exigencia de BT finita (~isnan(bt)) se queda.
    if apply_bt_gate:
        gate_bt = bt > t_bg + bt_sanity_k
    else:
        gate_bt = np.ones_like(roi_mask, dtype=bool)
    hot = (
        roi_mask
        & ~np.isnan(dnti)
        & ~np.isnan(bt)
        & (dnti > c1)
        & gate_bt
    )
```

- [ ] **Paso 4: implementar en `dual_roi_contextual_dnti_hot_mask`**

En la firma (`detection_context.py:332`), después de `deti: Optional[np.ndarray] = None,` agregar:

```python
    apply_bt_gate: bool = True,
```

Reemplazar `_kwargs` (`detection_context.py:365-371`):

```python
    _kwargs = dict(
        test1_mask=test1_mask,
        apply_unsuitable_filters=apply_unsuitable_filters,
        unsuitable_dnti_floor=unsuitable_dnti_floor,
        unsuitable_deti_floor=unsuitable_deti_floor,
        deti=deti,
    )
```

por:

```python
    _kwargs = dict(
        test1_mask=test1_mask,
        apply_unsuitable_filters=apply_unsuitable_filters,
        unsuitable_dnti_floor=unsuitable_dnti_floor,
        unsuitable_deti_floor=unsuitable_deti_floor,
        deti=deti,
        apply_bt_gate=apply_bt_gate,  # D22 (S142): mismo test, umbral dual
    )
```

- [ ] **Paso 5: implementar en `first_pass_tests_2_and_3`**

En la firma (`detection_context.py:422`), después de `use_prose_branch: bool = False,` agregar:

```python
    apply_bt_gate: bool = True,
```

Reemplazar `detection_context.py:527-533`:

```python
    # 5) Conjunción AND + ROI + bt sanity
    hot = (
        roi_mask
        & np.isfinite(dnti) & np.isfinite(deti)
        & pass_2 & pass_3
        & (bt > t_bg + bt_sanity_k)
    )
```

por:

```python
    # 5) Conjunción AND + ROI + bt sanity
    # D22 (S142): la fórmula de los Tests 2 y 3 (Coppola 2016a p. 7) no tiene condición de
    # temperatura. Con `apply_bt_gate=False` sólo se exige BT finita; el default conserva la
    # compuerta. mu y sigma NO dependen de esto (salen del pool de fondo, paso 3).
    gate_bt = (bt > t_bg + bt_sanity_k) if apply_bt_gate else np.isfinite(bt)
    hot = (
        roi_mask
        & np.isfinite(dnti) & np.isfinite(deti)
        & pass_2 & pass_3
        & gate_bt
    )
```

- [ ] **Paso 6: A49, revisar que no se comió nada**

```bash
git diff pipeline/detection_context.py
```

Comprobar a ojo que `return hot` (`contextual_dnti_hot_mask`), `return hot_summit | hot_scene` y `return hot, diag` siguen presentes y que las líneas antes y después de cada bloque están intactas.

- [ ] **Paso 7: correr en verde**

```bash
python -m pytest tests/test_d22_compuerta_bt_s142.py tests/test_second_pass_conditioned_s135.py tests/test_apagado_no_cambia_nada_s142.py::test_perfil_operacional_reproduce_el_golden_bit_a_bit -v
```

Esperado: los 7 tests de D22 PASS, los 17 de S135 PASS (17 según `tasks/BLOQUE_ARRANQUE_S136.md:15`; si el conteo real difiere, anotarlo sin cambiar el criterio: todos PASS) y el golden PASS.

- [ ] **Paso 8: suite completa**

```bash
python -m pytest -q -p no:cacheprovider 2>&1 | tail -3
```

Esperado: línea base de la Tarea 0 + los tests nuevos PASS, salvo los 3 de `test_apagado_no_cambia_nada_s142.py` que siguen rojos hasta la Tarea 6.

- [ ] **Paso 9: commit**

```bash
git add pipeline/detection_context.py tests/test_d22_compuerta_bt_s142.py
git commit -m "S142 D22: apply_bt_gate en los helpers del dNTI contextual y del primer pase (default = compuerta de hoy)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tarea 4: los dos flags y el parámetro en el perfil (OFF)

**Files:**
- Create: `tests/test_flags_d22_d25_perfil_s142.py`
- Modify: `pipeline/profile.py` (después de `:475`), `pipeline/profiles/mirova_equivalent.yaml` (después de `:30`, `:428`, `:442`), `tests/test_gr2_profile_invariants.py:91-163`

- [ ] **Paso 1: escribir los tests del perfil (rojos)**

```python
# -*- coding: utf-8 -*-
"""S142: los flags D22 y D25 existen, están OFF en el perfil operacional, se declaran en la sección
que el código LEE, y cada brazo del A/B lee lo que declara.

POR QUÉ LA SECCIÓN IMPORTA. `pipeline/profile.py` lee los `enable_*` de `paths:` (`_p`) y los
parámetros de `thresholds:` (`_t`). Una clave en la sección equivocada arranca en su default sin
síntoma y el A/B corre brazos idénticos (S124 con enable_utm_regrid, S133 "A/B sin sustrato").
"""
import os
import subprocess
import sys
from pathlib import Path

import pytest
import yaml

ROOT = Path(__file__).resolve().parents[1]
YAML_OP = ROOT / "pipeline" / "profiles" / "mirova_equivalent.yaml"
LEER = ("import pipeline.profile as p;"
        "print(p.ENABLE_TESTS_23_NO_BT_GATE_VIIRS375, p.ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375,"
        " p.ENABLE_SECOND_PASS_CONDITIONED, p.ENABLE_TEST1_CONTEXTUAL_KEEP_PEAK,"
        " p.ENABLE_SECOND_PASS_ADJACENT, p.ENABLE_TESTS_23_PROSE_BRANCH,"
        " p.ENABLE_LOCAL_CLUSTER_MAGNITUDE_VIIRS375, p.VRP_BG_NEIGHBOR_MAX_HALF_PX,"
        " p.SENSOR_MODIS, p.SENSOR_VIIRS_375, p.SENSOR_VIIRS_750, p.DATA_SUBDIR)")


def _leer_perfil(nombre):
    out = subprocess.run([sys.executable, "-c", LEER], cwd=str(ROOT), capture_output=True, text=True,
                         env={**os.environ, "VRP_PROFILE": nombre, "PYTHONIOENCODING": "utf-8"})
    assert out.returncode == 0, out.stderr[-2000:]
    campos = out.stdout.strip().splitlines()[-1].split()
    flags = tuple(c == "True" for c in campos[:7])
    return flags, int(campos[7]), tuple(c == "True" for c in campos[8:11]), campos[11]


def test_el_perfil_operacional_los_lleva_apagados():
    flags, max_half, _sens, subdir = _leer_perfil("mirova_equivalent")
    assert flags[0] is False and flags[1] is False
    assert max_half == 3
    assert subdir == "mirova_equivalent"


def test_el_yaml_los_declara_en_la_seccion_que_el_codigo_lee():
    cfg = yaml.safe_load(YAML_OP.read_text(encoding="utf-8"))
    for clave in ("enable_tests_23_no_bt_gate_viirs375", "enable_vrp_bg_neighbor_mean_viirs375"):
        assert clave in cfg["paths"], f"{clave} fuera de paths:"
        assert cfg["paths"][clave] is False
        assert clave not in cfg["thresholds"] and clave not in cfg
    assert cfg["thresholds"]["vrp_bg_neighbor_max_half_px"] == 3
    assert "vrp_bg_neighbor_max_half_px" not in cfg["paths"]


def test_max_half_invalido_falla_al_cargar(tmp_path):
    """Un perfil mal escrito falla con mensaje claro, no con un IndexError dentro del loop."""
    nombre = "_s142_tmp_max_half_cero"
    p = ROOT / "pipeline" / "profiles" / f"{nombre}.yaml"
    p.write_text("extends: mirova_equivalent\nthresholds:\n  vrp_bg_neighbor_max_half_px: 0\n"
                 f"output:\n  data_subdir: {nombre}\n", encoding="utf-8")
    try:
        out = subprocess.run([sys.executable, "-c", "import pipeline.profile"], cwd=str(ROOT),
                             capture_output=True, text=True,
                             env={**os.environ, "VRP_PROFILE": nombre, "PYTHONIOENCODING": "utf-8"})
        assert out.returncode != 0
        assert "vrp_bg_neighbor_max_half_px" in out.stderr
    finally:
        p.unlink()


# (no_gate, vecinos, conditioned, keep_peak, adjacent, prose, corona)
BRAZOS = {
    "_s142_ab_control":            (False, False, False, True,  True, False, False),
    "_s142_ab_literal":            (True,  True,  True,  False, True, False, False),
    "_s142_ab_lit_sin_fondo":      (True,  False, True,  False, True, False, False),
    "_s142_ab_lit_con_compuerta":  (False, True,  True,  False, True, False, False),
    "_s142_ab_lit_sp_suelto":      (True,  True,  False, False, True, False, False),
    "_s142_ab_lit_keep_peak":      (True,  True,  True,  True,  True, False, False),
}


@pytest.mark.parametrize("brazo,esperado", sorted(BRAZOS.items()))
def test_cada_brazo_lee_lo_que_declara(brazo, esperado):
    if not (ROOT / "pipeline" / "profiles" / f"{brazo}.yaml").exists():
        pytest.fail(f"falta el perfil {brazo} (Tarea 7)")
    flags, max_half, sensores, subdir = _leer_perfil(brazo)
    assert flags == esperado, f"{brazo}: declara {esperado}, lee {flags}"
    assert max_half == 3
    assert sensores == (False, True, False), f"{brazo}: el A/B de la Fase 1 es sólo VIIRS 375"
    assert subdir == brazo, f"{brazo}: escribe en {subdir}, no aislado"


def test_ningun_brazo_apaga_keep_peak_solo():
    """Spec §6: nunca keep_peak apagado solo (§1.3 del spec)."""
    for brazo, f in BRAZOS.items():
        solo_keep_peak_off = (not f[3]) and not (f[0] or f[1] or f[2])
        assert not solo_keep_peak_off, brazo


def test_el_perfil_de_verificacion_solo_enciende_los_dos_flags_nuevos():
    nombre = "_s142_verif_flags_nuevos"
    if not (ROOT / "pipeline" / "profiles" / f"{nombre}.yaml").exists():
        pytest.fail(f"falta el perfil {nombre} (Tarea 6)")
    flags, _mh, _s, subdir = _leer_perfil(nombre)
    _f_op, _mh2, _s2, _sub = _leer_perfil("mirova_equivalent")
    assert flags[:2] == (True, True)
    assert flags[2:] == _f_op[2:], "el perfil de verificación no puede cambiar otros flags"
    assert subdir == nombre
```

- [ ] **Paso 2: agregar al guard GR2**

En `tests/test_gr2_profile_invariants.py`, dentro de `EXPECTED_OPERATIONAL_FLAGS`, después de `"ENABLE_LOCAL_CLUSTER_MAGNITUDE": False,` (`:132`):

```python
    # S142 (A45): D22 sin compuerta de temperatura y D25 fondo por vecinos, ambos V375. OFF hasta
    # el A/B literal completo + ablaciones (plan docs/superpowers/plans/2026-09-15-flags-d22-d25.md).
    "ENABLE_TESTS_23_NO_BT_GATE_VIIRS375": False,
    "ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375": False,
```

Y en `const_to_yaml` (`:152-163`), después de `"ENABLE_NADIR_FIXED_PIXEL_AREA_VIIRS": "enable_nadir_fixed_pixel_area_viirs",`:

```python
        "ENABLE_TESTS_23_NO_BT_GATE_VIIRS375": "enable_tests_23_no_bt_gate_viirs375",
        "ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375": "enable_vrp_bg_neighbor_mean_viirs375",
```

- [ ] **Paso 3: correr y ver el rojo**

```bash
python -m pytest tests/test_flags_d22_d25_perfil_s142.py tests/test_gr2_profile_invariants.py -v
```

Esperado: FAIL en `test_el_perfil_operacional_los_lleva_apagados` (`AttributeError: module 'pipeline.profile' has no attribute 'ENABLE_TESTS_23_NO_BT_GATE_VIIRS375'`), en `test_el_yaml_los_declara...` (`KeyError`/assert), en `test_max_half_invalido_falla_al_cargar` (returncode 0), en los 6 brazos (falta perfil), en el de verificación (falta perfil) y en `test_operational_flag_value` / `test_profile_constants_match_yaml_paths` de GR2. `test_ningun_brazo_apaga_keep_peak_solo` PASS.

- [ ] **Paso 4: implementar en `pipeline/profile.py`**

Después de `profile.py:475` (`    _p.get("enable_local_cluster_magnitude_viirs375", False))`) insertar:

```python

# S142 D22: sin la compuerta de temperatura en los Tests 2 y 3 y en el dNTI contextual (VIIRS 375).
# POR QUÉ: la fórmula de los Tests 2 y 3 de Coppola 2016a (p. 7) no tiene condición de temperatura.
# La compuerta `bt > t_bg + NTI_BT_SANITY_K` nació para el camino del NTI absoluto y se heredó a los
# contextuales; en un cono nevado de noche descarta al píxel con lava sub-píxel que está más frío que
# la mediana del anillo. Alcance: sólo process_viirs.py (MODIS y VIIRS 750 no la reciben). NO toca el
# camino B (NTI > K1, D23). Solo no devuelve alertas (corrección S138: el segundo pase ya rescata esos
# píxeles); se evalúa en el A/B literal completo + ablaciones. OFF por A45.
ENABLE_TESTS_23_NO_BT_GATE_VIIRS375: bool = bool(
    _p.get("enable_tests_23_no_bt_gate_viirs375", False))

# S142 D25: fondo del VRP = media de la RADIANCIA de los vecinos NO alertados de cada píxel alertado
# (Coppola 2016a ec. 6; Fernandina 2025 p. 9; Campus 2024 p. 3; docs/MIROVA_DIVERGENCES.md D25).
# POR QUÉ: hoy el fondo es la mediana del anillo regional 5-25 km (o el kernel 3x3 opt-in de 5
# volcanes). En la cumbre helada el cráter queda más frío que esa mediana y el exceso se recorta a
# 0,0 MW aunque la detección lo haya aceptado. Precedencia: cuando está ON pisa al kernel opt-in y al
# anillo; el fondo de hoy queda como respaldo SÓLO para el píxel sin vecinos no alertados dentro de
# VRP_BG_NEIGHBOR_MAX_HALF_PX. El recorte a cero NO cambia (pregunta abierta al autor). VIIRS 375
# sólo. OFF por A45.
ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375: bool = bool(
    _p.get("enable_vrp_bg_neighbor_mean_viirs375", False))
# Semiancho máximo (en píxeles) de la ventana que se agranda cuando los 8 vecinos están todos
# alertados (interior de un cúmulo): 1 = sólo los 8 vecinos; 3 = hasta 7x7. DECISIÓN ABIERTA (el
# paper no trata el píxel sin vecinos no alertados); ver el plan S142 §5.
VRP_BG_NEIGHBOR_MAX_HALF_PX: int = int(_t.get("vrp_bg_neighbor_max_half_px", 3))
if VRP_BG_NEIGHBOR_MAX_HALF_PX < 1:
    raise ValueError(
        "vrp_bg_neighbor_max_half_px debe ser >= 1 (1 = los 8 vecinos); "
        f"got {VRP_BG_NEIGHBOR_MAX_HALF_PX}")
```

- [ ] **Paso 5: implementar en `pipeline/profiles/mirova_equivalent.yaml`**

Después de `mirova_equivalent.yaml:30` (`  test1_intermediate_bg_ring_km: [1.5, 3.0]`):

```yaml
  # S142 D25: semiancho máximo (px) de la ventana del fondo por vecinos cuando los 8 vecinos están
  # alertados. Sólo actúa con enable_vrp_bg_neighbor_mean_viirs375 (en paths:, OFF). Decisión
  # abierta: plan docs/superpowers/plans/2026-09-15-flags-d22-d25.md §5.
  vrp_bg_neighbor_max_half_px: 3
```

Después de `mirova_equivalent.yaml:428` (`  enable_second_pass_conditioned: false`):

```yaml
  # S142 D22: quita la compuerta bt > t_bg + nti_bt_sanity_k de los Tests 2 y 3 y del dNTI
  # contextual, SÓLO en VIIRS 375. La fórmula de Coppola 2016a (p. 7) no la tiene; en un cono
  # nevado de noche descarta al píxel con lava sub-píxel más frío que la mediana del anillo.
  # Sola no devuelve alertas (S138: el segundo pase ya rescata esos píxeles). OFF hasta el A/B
  # literal completo + ablaciones (A45).
  enable_tests_23_no_bt_gate_viirs375: false
```

Después de `mirova_equivalent.yaml:442` (`  enable_local_kernel_bg: true`):

```yaml
  # S142 D25: fondo del VRP = media de la radiancia de los vecinos NO alertados de cada píxel
  # alertado (Coppola 2016a ec. 6; Fernandina 2025 p. 9), en VIIRS 375. Con ON pisa al kernel
  # opt-in de arriba y a la mediana del anillo 5-25 km; el fondo de hoy queda como respaldo del
  # píxel sin vecinos no alertados. El recorte a cero no cambia. OFF hasta el A/B (A45).
  enable_vrp_bg_neighbor_mean_viirs375: false
```

**Ojo con los números de línea**: el primer insert (4 líneas en `thresholds:`) corre las líneas siguientes. Ubicar los inserts por contenido (`enable_second_pass_conditioned: false`, `enable_local_kernel_bg: true`), no por número.

- [ ] **Paso 6: correr en verde lo que corresponde**

```bash
python -m pytest tests/test_flags_d22_d25_perfil_s142.py tests/test_gr2_profile_invariants.py -v
```

Esperado: PASS en operacional, sección YAML, `max_half` inválido, `keep_peak` solo y GR2. FAIL en los 6 brazos y en el perfil de verificación (se crean en las Tareas 6 y 7).

- [ ] **Paso 7: suite completa y golden**

```bash
python -m pytest tests/test_apagado_no_cambia_nada_s142.py::test_perfil_operacional_reproduce_el_golden_bit_a_bit -v
python -m pytest -q -p no:cacheprovider 2>&1 | tail -3
```

Esperado: golden PASS. Suite = línea base + nuevos, con los rojos conocidos (verificación y brazos).

- [ ] **Paso 8: commit**

```bash
git add pipeline/profile.py pipeline/profiles/mirova_equivalent.yaml tests/test_flags_d22_d25_perfil_s142.py tests/test_gr2_profile_invariants.py
git commit -m "S142: flags ENABLE_TESTS_23_NO_BT_GATE_VIIRS375 y ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375 (OFF) y VRP_BG_NEIGHBOR_MAX_HALF_PX

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tarea 5: helper del fondo por vecinos en `vrp_regimes.py`

**Files:**
- Create: `tests/test_d25_fondo_vecinos_s142.py` (parte de helper)
- Modify: `pipeline/vrp_regimes.py` (después de `:106`)

- [ ] **Paso 1: escribir los tests del helper (rojos)**

```python
# -*- coding: utf-8 -*-
"""S142 D25: fondo del VRP = media de la radiancia de los vecinos NO alertados de cada píxel alertado.

EL FENÓMENO. El VRP resta a la radiancia del píxel caliente la del terreno que tendría sin el
foco. Hoy ese terreno es la mediana de un anillo de 5 a 25 km: en un cono nevado de noche es valle
tibio, más caliente que la cumbre, y el cráter con lava sub-píxel sale con exceso negativo,
recortado a 0,0 MW. MIROVA (Coppola 2016a ec. 6; Fernandina 2025 p. 9; Campus 2024 p. 3) usa los
píxeles que rodean a cada alertado y que no están alertados.

QUÉ FIJAN. (1) La media es de RADIANCIA, no Planck de la media de temperaturas (Planck es convexo en
el MIR y la diferencia crece con la heterogeneidad del entorno). (2) Los alertados vecinos quedan
fuera. (3) El píxel interior de un cúmulo busca afuera, hasta `max_half_px`. (4) NaN se ignora. (5)
El envoltorio de process_viirs usa el fondo de hoy sólo donde no hay vecinos, y lo cuenta.
"""
import json
import math
import re
import sys
from pathlib import Path

import numpy as np
import pytest

ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(ROOT))
sys.path.insert(0, str(ROOT / "tests"))

from pipeline.vrp_regimes import neighbor_mean_radiance_background  # noqa: E402

LAMBDA_I04 = 3.740


def _planck(bt):
    import pipeline.process_viirs as pv
    return pv.bt_to_spectral_radiance(np.asarray(bt, dtype=np.float64), pv.I04_LAMBDA)


def test_un_alertado_al_centro_es_la_media_de_radiancia_de_sus_8_vecinos():
    bt = np.full((5, 5), 260.0)
    bt[1:4, 1:4] = np.array([[250.0, 290.0, 250.0], [290.0, 330.0, 290.0], [250.0, 290.0, 250.0]])
    alert = np.zeros((5, 5), dtype=bool)
    alert[2, 2] = True
    l_bk, half = neighbor_mean_radiance_background(bt, alert, [2], [2], LAMBDA_I04, max_half_px=1)
    vecinos = [250.0, 290.0, 250.0, 290.0, 290.0, 250.0, 290.0, 250.0]
    esperado = float(np.mean(_planck(vecinos)))
    assert math.isclose(l_bk[0], esperado, rel_tol=1e-12)
    assert half[0] == 1
    # y NO es Planck de la media de temperaturas (270 K): Jensen en el MIR
    planck_media_bt = float(_planck(np.mean(vecinos)))
    assert not math.isclose(l_bk[0], planck_media_bt, rel_tol=1e-3)


def test_los_vecinos_alertados_quedan_fuera():
    bt = np.full((5, 5), 260.0)
    bt[2, 2] = 330.0
    bt[2, 3] = 320.0
    alert = np.zeros((5, 5), dtype=bool)
    alert[2, 2] = alert[2, 3] = True
    l_bk, half = neighbor_mean_radiance_background(bt, alert, [2, 2], [2, 3], LAMBDA_I04, max_half_px=1)
    assert math.isclose(l_bk[0], float(_planck(260.0)), rel_tol=1e-12)
    assert math.isclose(l_bk[1], float(_planck(260.0)), rel_tol=1e-12)
    assert list(half) == [1, 1]


def test_el_interior_de_un_cumulo_busca_afuera_hasta_max_half():
    bt = np.full((9, 9), 255.0)
    bt[3:6, 3:6] = 300.0
    bt[4, 4] = 320.0
    alert = np.zeros((9, 9), dtype=bool)
    alert[3:6, 3:6] = True
    l1, h1 = neighbor_mean_radiance_background(bt, alert, [4], [4], LAMBDA_I04, max_half_px=1)
    assert math.isnan(l1[0]) and h1[0] == 0
    l3, h3 = neighbor_mean_radiance_background(bt, alert, [4], [4], LAMBDA_I04, max_half_px=3)
    assert h3[0] == 2
    assert math.isclose(l3[0], float(_planck(255.0)), rel_tol=1e-12)


def test_nan_se_ignora_y_sin_vecinos_validos_da_nan():
    bt = np.full((3, 3), np.nan)
    bt[1, 1] = 300.0
    bt[0, 0] = 260.0
    alert = np.zeros((3, 3), dtype=bool)
    alert[1, 1] = True
    l_bk, half = neighbor_mean_radiance_background(bt, alert, [1], [1], LAMBDA_I04, max_half_px=1)
    assert math.isclose(l_bk[0], float(_planck(260.0)), rel_tol=1e-12) and half[0] == 1
    bt[0, 0] = np.nan
    l_bk, half = neighbor_mean_radiance_background(bt, alert, [1], [1], LAMBDA_I04, max_half_px=3)
    assert math.isnan(l_bk[0]) and half[0] == 0


def test_borde_de_la_escena_no_envuelve():
    bt = np.full((4, 4), 250.0)
    bt[0, 3] = 290.0   # esquina opuesta en columna: no puede ser vecina de (0, 0)
    bt[0, 0] = 330.0
    alert = np.zeros((4, 4), dtype=bool)
    alert[0, 0] = True
    l_bk, _ = neighbor_mean_radiance_background(bt, alert, [0], [0], LAMBDA_I04, max_half_px=1)
    assert math.isclose(l_bk[0], float(_planck(250.0)), rel_tol=1e-12)


def test_entradas_invalidas_fallan():
    bt = np.full((3, 3), 260.0)
    with pytest.raises(ValueError):
        neighbor_mean_radiance_background(bt, np.zeros((3, 3), bool), [1], [1], LAMBDA_I04, max_half_px=0)
    with pytest.raises(ValueError):
        neighbor_mean_radiance_background(bt, np.zeros((2, 2), bool), [1], [1], LAMBDA_I04)
    with pytest.raises(ValueError):
        neighbor_mean_radiance_background(bt, np.zeros((3, 3), bool), [1, 2], [1], LAMBDA_I04)


def test_sin_alertados_devuelve_arreglos_vacios():
    l_bk, half = neighbor_mean_radiance_background(np.full((3, 3), 260.0), np.zeros((3, 3), bool),
                                                   [], [], LAMBDA_I04)
    assert l_bk.shape == (0,) and half.shape == (0,)
```

- [ ] **Paso 2: correr y ver el rojo**

```bash
python -m pytest tests/test_d25_fondo_vecinos_s142.py -v
```

Esperado: error de colección `ImportError: cannot import name 'neighbor_mean_radiance_background' from 'pipeline.vrp_regimes'`.

- [ ] **Paso 3: implementar el helper**

En `pipeline/vrp_regimes.py`, entre `    return t_bks` (`:106`) y `def cluster_corona_background(` (`:109`), insertar (dejando dos líneas en blanco antes y después):

```python
def neighbor_mean_radiance_background(
    bt_grid: np.ndarray,
    alert_mask: np.ndarray,
    hot_rows: Sequence[int],
    hot_cols: Sequence[int],
    wavelength_um: float,
    *,
    max_half_px: int = 1,
) -> tuple[np.ndarray, np.ndarray]:
    """Fondo MIR de cada píxel alertado = media de la RADIANCIA de sus vecinos no alertados (D25, S142).

    POR QUÉ. Coppola 2016a (ec. 6) resta como fondo la media aritmética de los píxeles que rodean
    al activo; Fernandina 2025 (p. 9) precisa que son los vecinos NO alertados, y Campus 2024 (p. 3)
    que cada alertado tiene su propio fondo (docs/MIROVA_DIVERGENCES.md D25). Nuestro pipeline usa
    la mediana de un anillo regional de 5 a 25 km, que en un cono nevado de noche es valle tibio:
    el cráter con lava sub-píxel sale más frío que ese fondo y su VRP se recorta a cero.

    QUÉ SE PROMEDIA. Radiancia (Planck de cada vecino), no temperatura: los textos hablan de
    radiancia, y como Planck es convexo en el MIR, Planck(media de BT) subestima el fondo cuando el
    entorno es heterogéneo. Difiere de `compute_local_background` (arriba), que promedia BT.

    PÍXEL SIN VECINOS NO ALERTADOS (interior de un cúmulo). El paper no lo trata. Se agranda la
    ventana (5x5, 7x7, ...) hasta `max_half_px`, porque los píxeles no alertados más cercanos son la
    corona del cúmulo, que es lo que "surrounding" describe para un cuerpo de varios píxeles. Si no
    hay ninguno, devuelve NaN y el llamador decide el respaldo. DECISIÓN ABIERTA (plan S142 §5).

    Args:
        bt_grid: BT en K (2D). NaN = inválido, se ignora.
        alert_mask: bool 2D, True en TODOS los píxeles alertados (no sólo los de hot_rows/cols).
        hot_rows, hot_cols: índices de los píxeles a los que se les calcula fondo.
        wavelength_um: longitud de onda de la banda MIR (VIIRS I04 = 3,740).
        max_half_px: semiancho máximo de la ventana (>= 1; 1 = los 8 vecinos).

    Returns:
        (l_bk, half_used): radiancia de fondo (W/m2/sr/µm, float64; NaN sin vecinos) y el semiancho
        con que se obtuvo (0 = sin vecinos), un valor por píxel.
    """
    if max_half_px < 1:
        raise ValueError(f"max_half_px debe ser >= 1, recibido {max_half_px}")
    grid = np.asarray(bt_grid, dtype=np.float64)
    alert = np.asarray(alert_mask, dtype=bool)
    if grid.shape != alert.shape:
        raise ValueError(f"bt_grid {grid.shape} y alert_mask {alert.shape} difieren")
    if len(hot_rows) != len(hot_cols):
        raise ValueError("hot_rows y hot_cols deben tener mismo largo")

    n_rows, n_cols = grid.shape
    n = len(hot_rows)
    l_bk = np.full(n, np.nan, dtype=np.float64)
    half_used = np.zeros(n, dtype=int)
    for i, (r, c) in enumerate(zip(hot_rows, hot_cols)):
        r, c = int(r), int(c)
        for half in range(1, max_half_px + 1):
            r0, r1 = max(0, r - half), min(n_rows, r + half + 1)
            k0, k1 = max(0, c - half), min(n_cols, c + half + 1)
            ventana = grid[r0:r1, k0:k1]
            usable = ~alert[r0:r1, k0:k1] & np.isfinite(ventana)
            if np.any(usable):
                vals = ventana[usable]
                with np.errstate(invalid="ignore", divide="ignore", over="ignore"):
                    rad = C1 / (wavelength_um ** 5 * (np.exp(C2 / (wavelength_um * vals)) - 1))
                l_bk[i] = float(np.mean(rad))
                half_used[i] = half
                break
    return l_bk, half_used
```

(`C1`, `C2` y `Sequence` ya están importados en `vrp_regimes.py:30` y `:35`. La expresión de Planck es la misma, en el mismo orden, que `process_viirs.bt_to_spectral_radiance`, `process_viirs.py:235-238`.)

- [ ] **Paso 4: correr en verde**

```bash
python -m pytest tests/test_d25_fondo_vecinos_s142.py tests/test_local_kernel_background.py -v
```

Esperado: 7 PASS nuevos y los 8 de `test_local_kernel_background.py` PASS.

- [ ] **Paso 5: actualizar la cabecera FICHA de `vrp_regimes.py`**

Reemplazar `vrp_regimes.py:11`:

```python
# Variables     : VRP (MW), BT del píxel, T de fondo (corona/local), área del píxel.
```

por:

```python
# Variables     : VRP (MW), BT del píxel, T de fondo (corona/local; media de radiancia de
#                 vecinos no alertados D25, flag OFF en producción), área del píxel.
```

- [ ] **Paso 6: suite completa y golden**

```bash
python -m pytest tests/test_apagado_no_cambia_nada_s142.py::test_perfil_operacional_reproduce_el_golden_bit_a_bit -q
python -m pytest -q -p no:cacheprovider 2>&1 | tail -3
```

Esperado: golden PASS; suite = línea base + nuevos, rojos conocidos sólo en verificación y brazos.

- [ ] **Paso 7: commit**

```bash
git add pipeline/vrp_regimes.py tests/test_d25_fondo_vecinos_s142.py
git commit -m "S142 D25: helper neighbor_mean_radiance_background (media de radiancia de vecinos no alertados)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tarea 6: cableado en `process_viirs.py` y perfil de verificación

**Files:**
- Create: `pipeline/profiles/_s142_verif_flags_nuevos.yaml`
- Modify: `pipeline/process_viirs.py` (ver §2.2)
- Modify: `tests/test_d22_compuerta_bt_s142.py`, `tests/test_d25_fondo_vecinos_s142.py` (tests de extremo a extremo y guards de fuente)

- [ ] **Paso 1: agregar al final de `tests/test_d22_compuerta_bt_s142.py` los tests de extremo a extremo y de fuente (rojos)**

```python
@pytest.fixture(scope="module")
def v375_sin_compuerta(tmp_path_factory):
    texto = correr_en_subproceso(tmp_path_factory.mktemp("d22"), perfil="mirova_equivalent",
                                 parches=("ENABLE_TESTS_23_NO_BT_GATE_VIIRS375=True",), solo="v375")
    return json.loads(texto)["v375"]


def test_extremo_a_extremo_el_primer_pase_toma_lo_que_rescataba_el_segundo(v375_sin_compuerta):
    """Esperado medido con el probe S142 (compuerta simulada en -inf): el primer pase pasa de 0 a 2
    píxeles, el segundo de 2 a 0, y lo publicado no cambia. Es la corrección S138: sola, D22 no
    devuelve alertas ni mueve la magnitud."""
    hoy = GOLDEN["v375"]["nevado_vecino_tibio|kernel=False"]
    on = v375_sin_compuerta["nevado_vecino_tibio|kernel=False"]
    assert (hoy["diag_n_first_pass_pixels"], hoy["diag_n_second_pass_recapture"]) == (0, 2)
    assert (on["diag_n_first_pass_pixels"], on["diag_n_second_pass_recapture"]) == (2, 0)
    assert on["anomaly_pixels"] == hoy["anomaly_pixels"]
    assert on["primary_cluster"] == hoy["primary_cluster"]


def test_la_escena_plana_no_cambia_sin_compuerta(v375_sin_compuerta):
    for k in ("plana|kernel=False", "plana|kernel=True"):
        assert json.dumps(v375_sin_compuerta[k], sort_keys=True) == json.dumps(GOLDEN["v375"][k], sort_keys=True), k


def test_fuente_viirs375_pasa_el_flag_en_las_tres_llamadas_y_en_el_camino_eti():
    s = _src("pipeline/process_viirs.py")
    assert _tokens(s, "apply_bt_gate=not ENABLE_TESTS_23_NO_BT_GATE_VIIRS375") == 3
    assert re.search(r"_eti_gate_bt = \(np\.ones_like\(bt, dtype=bool\) if ENABLE_TESTS_23_NO_BT_GATE_VIIRS375\b", s)
    # el camino B (NTI > K1, Test 1 del paper, D23) conserva su compuerta intacta
    assert re.search(r"& \(nti > NTI_K1_NIGHT\)\s*\n\s*& \(bt > \(t_bg_i04 \+ NTI_BT_SANITY_K\)\)", s)


@pytest.mark.parametrize("rel", ["pipeline/process_modis.py", "pipeline/process_viirs_mod.py"])
def test_modis_y_viirs750_no_conocen_el_flag_ni_el_parametro(rel):
    s = _src(rel)
    assert _tokens(s, "ENABLE_TESTS_23_NO_BT_GATE_VIIRS375") == 0, rel
    assert _tokens(s, "apply_bt_gate") == 0, rel
```

Nota A92: `_tokens` exige frontera de palabra a los dos lados, así que `...VIIRS375` no puede quedar satisfecho por un prefijo ni por un nombre más largo. La cuenta 3 corresponde a las llamadas de las líneas de hoy 1034, 1045 y 1240.

- [ ] **Paso 2: agregar al final de `tests/test_d25_fondo_vecinos_s142.py` los tests del envoltorio, de extremo a extremo y de fuente (rojos)**

```python
from arnes_sintetico_s142 import CENTRO_V375, correr_en_subproceso, escena_v375  # noqa: E402

GOLDEN = json.loads((ROOT / "tests" / "golden_s142" / "apagado.json").read_text(encoding="utf-8"))


def _src(rel):
    return (ROOT / rel).read_text(encoding="utf-8")


def test_envoltorio_usa_el_fondo_de_hoy_solo_sin_vecinos_y_lo_cuenta():
    import pipeline.process_viirs as pv
    bt = np.full((9, 9), 255.0)
    bt[3:6, 3:6] = 300.0
    alert = np.zeros((9, 9), dtype=bool)
    alert[3:6, 3:6] = True
    rows, cols = [4, 3], [4, 3]
    l_bg, n_sin = pv.vrp_bg_neighbor_mean_v375(bt, alert, rows, cols, np.float64(0.123), max_half_px=1)
    assert n_sin == 1
    assert l_bg[0] == 0.123                                   # interior: respaldo escalar
    assert math.isclose(l_bg[1], float(_planck(255.0)), rel_tol=1e-12)   # borde: vecinos
    legado = np.array([0.5, 0.6])
    l_bg2, n_sin2 = pv.vrp_bg_neighbor_mean_v375(bt, alert, rows, cols, legado, max_half_px=1)
    assert n_sin2 == 1 and l_bg2[0] == 0.5                    # respaldo por píxel


@pytest.fixture(scope="module")
def v375_vecinos(tmp_path_factory):
    texto = correr_en_subproceso(tmp_path_factory.mktemp("d25"), perfil="mirova_equivalent",
                                 parches=("ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375=True",), solo="v375")
    return json.loads(texto)["v375"]


def _esperado_vrp(bt4, alert, r, c):
    import pipeline.process_viirs as pv
    ventana = bt4[r - 1:r + 2, c - 1:c + 2]
    usable = ~alert[r - 1:r + 2, c - 1:c + 2] & np.isfinite(ventana)
    l_bk = float(np.mean(_planck(ventana[usable])))
    l_hot = float(pv.bt_to_spectral_radiance(np.float64(bt4[r, c]), pv.I04_LAMBDA))
    area = pv.NADIR_PIXEL_AREA_M2   # nadir fijo en producción (ENABLE_NADIR_FIXED_PIXEL_AREA_VIIRS)
    return area * pv.WOOSTER_COEFF * max(l_hot - l_bk, 0.0) / 1e6


def test_extremo_a_extremo_el_crater_deja_de_publicarse_en_cero(v375_vecinos):
    """Esperado (probe S142): el cráter pasa de 0,0 a ~0,0968 MW con 7 vecinos no alertados."""
    bands, _geo = escena_v375("nevado_vecino_tibio")
    bt4 = bands["I04"].astype(np.float64)
    c = CENTRO_V375
    alert = np.zeros(bt4.shape, dtype=bool)
    alert[c, c] = alert[c, c + 1] = True
    hoy = GOLDEN["v375"]["nevado_vecino_tibio|kernel=False"]
    on = v375_vecinos["nevado_vecino_tibio|kernel=False"]
    assert [p["vrp_mw"] for p in hoy["anomaly_pixels"]] == [0.0, 0.0]
    por_bt = {p["bt_k"]: p["vrp_mw"] for p in on["anomaly_pixels"]}
    assert math.isclose(por_bt[266.0], _esperado_vrp(bt4, alert, c, c), abs_tol=6e-5)
    assert math.isclose(por_bt[262.0], _esperado_vrp(bt4, alert, c, c + 1), abs_tol=6e-5)
    assert por_bt[266.0] > 0.09
    # la detección y el fondo del anillo no se tocan: sólo la resta
    assert on["t_bg_k"] == hoy["t_bg_k"] and on["n_anomalous_pixels"] == hoy["n_anomalous_pixels"]
    assert on["diag_bg_vecinos_n_sin_vecinos"] == 0
    assert on["diag_L_bg_vecinos_w_m2_sr_um"] is not None
    assert "diag_bg_vecinos_n_sin_vecinos" not in hoy


def test_con_el_flag_on_el_kernel_opt_in_no_manda(v375_vecinos):
    """Precedencia: con vecinos disponibles, un volcán opt-in del kernel 3x3 publica lo mismo que uno
    que no lo es (el fondo nuevo pisa al kernel)."""
    a = v375_vecinos["nevado_vecino_tibio|kernel=False"]["anomaly_pixels"]
    b = v375_vecinos["nevado_vecino_tibio|kernel=True"]["anomaly_pixels"]
    assert a == b


def test_fuente_el_bloque_nuevo_va_despues_del_legado_y_el_recorte_sigue():
    s = _src("pipeline/process_viirs.py")
    legado = re.search(r"^\s*if ENABLE_LOCAL_KERNEL_BG and local_kernel_bg_compatible:", s, flags=re.M)
    nuevo = re.search(r"^\s*if ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375:\s*$", s, flags=re.M)
    recorte = re.search(r"^\s*delta_L = np\.maximum\(L_hot - L_bg, 0\.0\)", s, flags=re.M)
    assert legado and nuevo and recorte
    assert legado.start() < nuevo.start() < recorte.start()
    # los dos bloques del Test 1 usan el fondo por vecinos bajo el flag
    assert len(re.findall(r"_t1_Lbg, _n_sin = vrp_bg_neighbor_mean_v375\(", s)) == 2
    assert len(re.findall(r"np\.maximum\(t1_L - _t1_Lbg, 0\.0\)", s)) == 2


@pytest.mark.parametrize("rel", ["pipeline/process_modis.py", "pipeline/process_viirs_mod.py"])
def test_modis_y_viirs750_no_conocen_el_fondo_por_vecinos(rel):
    s = _src(rel)
    for token in ("ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375", "neighbor_mean_radiance_background",
                  "vrp_bg_neighbor_mean_v375", "VRP_BG_NEIGHBOR_MAX_HALF_PX"):
        assert not re.search(r"(?<![A-Za-z0-9_])" + token + r"(?![A-Za-z0-9_])", s), (rel, token)
```

- [ ] **Paso 3: correr y ver el rojo**

```bash
python -m pytest tests/test_d22_compuerta_bt_s142.py tests/test_d25_fondo_vecinos_s142.py -v
```

Esperado: los fixtures de subproceso fallan con `AttributeError: <module 'pipeline.process_viirs'> does not have the attribute 'ENABLE_TESTS_23_NO_BT_GATE_VIIRS375'` (y el equivalente para D25); los guards de fuente fallan; `test_envoltorio...` falla con `AttributeError: ... 'vrp_bg_neighbor_mean_v375'`. Los tests de MODIS/V750 PASS.

- [ ] **Paso 4: imports en `process_viirs.py`**

Después de `process_viirs.py:215` (`                               select_test1_effective_lbg)`) insertar:

```python
# S142 D22/D25: bloque aparte, DESPUÉS de la l. 212, para no correr las citas file:line que
# vigila el contrato G8 (tests/test_guard_declarado_vs_efectivo_s131.py: l. 83 y 212).
from pipeline.profile import (
    ENABLE_TESTS_23_NO_BT_GATE_VIIRS375,
    ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375,
    VRP_BG_NEIGHBOR_MAX_HALF_PX,
)
from .vrp_regimes import neighbor_mean_radiance_background  # S142 D25
```

- [ ] **Paso 5: envoltorio**

Después de `    return sum_cluster_vrp(per_pixel), False, per_pixel` (fin de `apply_corona_magnitude_v375`) insertar, con dos líneas en blanco antes de `def calculate_vrp`:

```python


def vrp_bg_neighbor_mean_v375(bt_grid, alert_mask, hot_rows, hot_cols, legacy_l_bg, *, max_half_px):
    """Fondo MIR por píxel para el VRP de VIIRS 375 con el flag D25 encendido (S142).

    POR QUÉ UN ENVOLTORIO. El helper de vrp_regimes devuelve NaN para el píxel sin vecinos no
    alertados dentro de `max_half_px`. Acá se decide el respaldo: el fondo que el bloque usaría HOY
    (`legacy_l_bg`: mediana del anillo, kernel opt-in o el L_bg efectivo del Test 1). Así el flag
    nunca deja a un píxel sin fondo y el respaldo queda contado en el record.

    Returns:
        (l_bg, n_sin_vecinos): radiancia de fondo por píxel (float64) y cuántos usaron el respaldo.
    """
    l_bk, _half = neighbor_mean_radiance_background(
        bt_grid, alert_mask, hot_rows, hot_cols, I04_LAMBDA, max_half_px=max_half_px)
    respaldo = np.broadcast_to(np.asarray(legacy_l_bg, dtype=np.float64), l_bk.shape)
    sin_vecinos = ~np.isfinite(l_bk)
    return np.where(sin_vecinos, respaldo, l_bk), int(np.count_nonzero(sin_vecinos))
```

- [ ] **Paso 6: inicialización de los diagnósticos**

Después de `    diag_L_bg_local = None  # S140 T7: sólo si actúa el kernel local` (hoy `process_viirs.py:897`) insertar:

```python
    # S142 D25: sólo se escriben al record con el flag ON (record idéntico con OFF).
    diag_L_bg_vecinos = None
    _bg_vecinos_n_sin_vecinos = 0
```

- [ ] **Paso 7: D22 en las tres llamadas y en el camino ETI**

En la llamada a `dual_roi_contextual_dnti_hot_mask` (hoy `:1034-1043`), reemplazar:

```python
                        bt_sanity_k=NTI_BT_SANITY_K,
                        apply_unsuitable_filters=ENABLE_UNSUITABLE_FILTERS_267_273,
                    )
                else:
```

por:

```python
                        bt_sanity_k=NTI_BT_SANITY_K,
                        apply_unsuitable_filters=ENABLE_UNSUITABLE_FILTERS_267_273,
                        apply_bt_gate=not ENABLE_TESTS_23_NO_BT_GATE_VIIRS375,  # D22 (S142)
                    )
                else:
```

En la llamada a `contextual_dnti_hot_mask` (hoy `:1045-1051`), reemplazar:

```python
                        c1=DNTI_CONTEXTUAL_C1,
                        bt_sanity_k=NTI_BT_SANITY_K,
                        apply_unsuitable_filters=ENABLE_UNSUITABLE_FILTERS_267_273,
                    )
```

por:

```python
                        c1=DNTI_CONTEXTUAL_C1,
                        bt_sanity_k=NTI_BT_SANITY_K,
                        apply_unsuitable_filters=ENABLE_UNSUITABLE_FILTERS_267_273,
                        apply_bt_gate=not ENABLE_TESTS_23_NO_BT_GATE_VIIRS375,  # D22 (S142)
                    )
```

En el camino ETI (hoy `:1180-1181`), reemplazar:

```python
                eti_path_hot = (eti_path_hot & roi_mask & ~np.isnan(bt)
                                & (bt > (t_bg_i04 + NTI_BT_SANITY_K)))
```

por:

```python
                # D22 (S142): este camino son los Tests 2 y 3 del paper (vía second_pass_adjacent
                # con el conjunto vacío); con el flag ON pierde la compuerta igual que el primer
                # pase. Inerte en producción: ENABLE_ETI_QUADRATIC_SCENE = False.
                _eti_gate_bt = (np.ones_like(bt, dtype=bool) if ENABLE_TESTS_23_NO_BT_GATE_VIIRS375
                                else (bt > (t_bg_i04 + NTI_BT_SANITY_K)))
                eti_path_hot = (eti_path_hot & roi_mask & ~np.isnan(bt)
                                & _eti_gate_bt)
```

En la llamada a `first_pass_tests_2_and_3` (hoy `:1240-1261`), reemplazar:

```python
                    unsuitable_deti_floor=_unsuit_deti,
                    use_prose_branch=ENABLE_TESTS_23_PROSE_BRANCH,
                )
                hot_mask_2d = fp_hot
```

por:

```python
                    unsuitable_deti_floor=_unsuit_deti,
                    use_prose_branch=ENABLE_TESTS_23_PROSE_BRANCH,
                    # D22 (S142): la fórmula de los Tests 2 y 3 (Coppola 2016a p. 7) no tiene
                    # compuerta de temperatura. El segundo pase nunca la tuvo (S138).
                    apply_bt_gate=not ENABLE_TESTS_23_NO_BT_GATE_VIIRS375,
                )
                hot_mask_2d = fp_hot
```

- [ ] **Paso 8: D25 en el bloque contextual**

Reemplazar (hoy `:1412-1413`; la línea `L_bg = bt_to_spectral_radiance(np.float64(t_bg_i04), I04_LAMBDA)` es única en el archivo):

```python
                else:
                    L_bg = bt_to_spectral_radiance(np.float64(t_bg_i04), I04_LAMBDA)
```

por (la línea en blanco y el comentario `# S26: clip a 0 ...` que siguen en el archivo quedan intactos debajo):

```python
                else:
                    L_bg = bt_to_spectral_radiance(np.float64(t_bg_i04), I04_LAMBDA)

                # D25 (S142): fondo = media de la radiancia de los vecinos NO alertados de cada
                # píxel alertado (Coppola 2016a ec. 6; Fernandina 2025 p. 9; Campus 2024 p. 3).
                # POR QUÉ: en la cumbre nevada, de noche, la mediana del anillo 5-25 km es valle
                # tibio y deja al cráter con exceso negativo, recortado a 0,0 MW. Va DESPUÉS del
                # bloque de arriba a propósito: su resultado es el respaldo del píxel sin vecinos
                # no alertados, y así no se mueven las líneas que vigila
                # tests/test_fondo_persistido_s140.py. Los alertados son hot_mask_2d entero.
                if ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375:
                    L_bg, _n_sin = vrp_bg_neighbor_mean_v375(
                        bt, hot_mask_2d, hot_rows, hot_cols, L_bg,
                        max_half_px=VRP_BG_NEIGHBOR_MAX_HALF_PX)
                    _bg_vecinos_n_sin_vecinos += _n_sin
                    diag_L_bg_vecinos = redondear_diag(float(np.nanmedian(L_bg)))
```

- [ ] **Paso 9: D25 en los dos bloques del Test 1**

Primer bloque (hoy `:1868-1869`), reemplazar:

```python
            # S33 D4: usar effective_L_bg (local default, global con flag ON).
            t1_delta_L = np.maximum(t1_L - effective_L_bg, 0.0)
```

por:

```python
            # S33 D4: usar effective_L_bg (local default, global con flag ON).
            _t1_Lbg = effective_L_bg
            if ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375:
                # D25 (S142): mismo fondo por vecinos que el bloque contextual. Alertados = cúmulo
                # final más los píxeles del Test 1 que se suman. Respaldo = effective_L_bg.
                _t1_Lbg, _n_sin = vrp_bg_neighbor_mean_v375(
                    bt, np.asarray(hot_mask_2d, dtype=bool) | np.asarray(test1_hot_filtered, dtype=bool),
                    t1_rows, t1_cols, effective_L_bg, max_half_px=VRP_BG_NEIGHBOR_MAX_HALF_PX)
            t1_delta_L = np.maximum(t1_L - _t1_Lbg, 0.0)
```

Segundo bloque (hoy `:1892-1895`), reemplazar:

```python
            t1_L = bt_to_spectral_radiance(t1_bt, I04_LAMBDA)
            t1_delta_L = np.maximum(t1_L - effective_L_bg, 0.0)
            t1_area = pixel_areas[t1_rows, t1_cols]
            t1_vrp_arr = t1_area * WOOSTER_COEFF * t1_delta_L / 1e6
```

por:

```python
            t1_L = bt_to_spectral_radiance(t1_bt, I04_LAMBDA)
            _t1_Lbg = effective_L_bg
            if ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375:
                # D25 (S142): ídem bloque anterior. Este es el que reconstruye anomaly_pixels
                # (lo que suma F5 en store.py), así que el conteo de respaldos se hace acá.
                _t1_Lbg, _n_sin = vrp_bg_neighbor_mean_v375(
                    bt, np.asarray(hot_mask_2d, dtype=bool) | np.asarray(test1_hot_filtered, dtype=bool),
                    t1_rows, t1_cols, effective_L_bg, max_half_px=VRP_BG_NEIGHBOR_MAX_HALF_PX)
                _bg_vecinos_n_sin_vecinos += _n_sin
            t1_delta_L = np.maximum(t1_L - _t1_Lbg, 0.0)
            t1_area = pixel_areas[t1_rows, t1_cols]
            t1_vrp_arr = t1_area * WOOSTER_COEFF * t1_delta_L / 1e6
```

**SOSPECHA a verificar en este paso**: que `hot_mask_2d` esté siempre definido cuando corren estos bloques. Razonamiento (leído, no ejecutado): los dos bloques exigen `test1_n_contrib > 0`, que sólo se asigna (`:1113`) dentro de la rama `if _t_bg_tmp is not None` (`:925`), la misma que asigna `hot_mask_2d` (`:1191`). Para confirmarlo, correr la suite de Test 1 (`python -m pytest tests -q -k "test1"`) y buscar `UnboundLocalError`.

- [ ] **Paso 10: diagnósticos al record**

Antes de `    return record` (hoy `:2131`), después del bloque `if ENABLE_HONEST_ANCHOR and _ha_nti_peak is not None:`, insertar:

```python

    # S142 D25: diagnóstico del fondo por vecinos. Con el flag OFF los campos NO aparecen y el
    # record queda idéntico al de hoy (tests/test_apagado_no_cambia_nada_s142.py).
    # diag_bg_vecinos_n_sin_vecinos cuenta evaluaciones de píxel que cayeron al respaldo (bloque
    # contextual + bloque Test 1 que reconstruye anomaly_pixels).
    if ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375:
        record["diag_L_bg_vecinos_w_m2_sr_um"] = diag_L_bg_vecinos
        record["diag_bg_vecinos_n_sin_vecinos"] = int(_bg_vecinos_n_sin_vecinos)
```

- [ ] **Paso 11: perfil de verificación**

Crear `pipeline/profiles/_s142_verif_flags_nuevos.yaml`:

```yaml
# S142: instrumento de tests/test_apagado_no_cambia_nada_s142.py. NO es un brazo del A/B.
#
# Enciende SOLO los dos flags nuevos (D22 y D25, VIIRS 375) para comprobar, corriendo el arnés
# sintético con este perfil, que MODIS y VIIRS 750 no se mueven, y que VIIRS 375 sí (control de
# instrumento: si V375 tampoco cambiara, el perfil no estaría leyendo sus flags).
#
# NO OPERACIONAL: data_subdir aislado, fuera del cron, nunca se reprocesa con él.
extends: mirova_equivalent
profile: _s142_verif_flags_nuevos
description: >
  Instrumento de test S142: sólo enable_tests_23_no_bt_gate_viirs375 y enable_vrp_bg_neighbor_mean_viirs375 en true.

paths:
  enable_tests_23_no_bt_gate_viirs375: true
  enable_vrp_bg_neighbor_mean_viirs375: true

output:
  data_subdir: _s142_verif_flags_nuevos
```

- [ ] **Paso 12: A49, revisar el diff**

```bash
git diff pipeline/process_viirs.py
```

Confirmar que `return vrp_mw, True, None`, `return sum_cluster_vrp(per_pixel), False, per_pixel`, `hot_mask_2d = fp_hot`, `delta_L = np.maximum(L_hot - L_bg, 0.0)`, `t1_vrp = t1_area * WOOSTER_COEFF * t1_delta_L / 1e6`, `anomaly_pixels = build_anomaly_pixels(t1_vrp_2d, lat, lon, dist, bt)` y `return record` siguen presentes, y que no se borró ninguna línea que no figure en los reemplazos.

- [ ] **Paso 13: correr en verde**

```bash
python -m pytest tests/test_d22_compuerta_bt_s142.py tests/test_d25_fondo_vecinos_s142.py tests/test_apagado_no_cambia_nada_s142.py tests/test_fondo_persistido_s140.py tests/test_corona_eq6_viirs_s126.py tests/test_corona_single_pixel_coherencia_s127.py -v
python -m pytest tests/test_flags_d22_d25_perfil_s142.py::test_el_perfil_de_verificacion_solo_enciende_los_dos_flags_nuevos -v
```

Esperado: todo PASS. En particular, `test_perfil_operacional_reproduce_el_golden_bit_a_bit` PASS (apagado bit a bit), `test_modis_y_viirs750_no_cambian_con_los_dos_flags_on` PASS, `test_el_perfil_de_verificacion_si_mueve_viirs375` PASS y los 4 tests de `test_fondo_persistido_s140.py` PASS.

- [ ] **Paso 14: guards por subcadena (A92) y suite completa**

```bash
PYTHONIOENCODING=utf-8 python experiments/_s133/auditar_guards_por_subcadena.py | tail -3
git checkout -- experiments/_s133/auditar_guards_por_subcadena.json
python -m pytest -q -p no:cacheprovider 2>&1 | tail -3
```

Esperado: `candidatos a falso verde: 0`. Suite = línea base + nuevos, con los 6 brazos en rojo (Tarea 7).

- [ ] **Paso 15: commit**

```bash
git add pipeline/process_viirs.py pipeline/profiles/_s142_verif_flags_nuevos.yaml tests/test_d22_compuerta_bt_s142.py tests/test_d25_fondo_vecinos_s142.py
git commit -m "S142: cablear D22 y D25 en process_viirs (OFF), perfil de verificación y tests de extremo a extremo

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tarea 7: perfiles de los brazos del A/B (sólo YAML)

**Files:**
- Create: `pipeline/profiles/_s142_ab_control.yaml`, `_s142_ab_literal.yaml`, `_s142_ab_lit_sin_fondo.yaml`, `_s142_ab_lit_con_compuerta.yaml`, `_s142_ab_lit_sp_suelto.yaml`, `_s142_ab_lit_keep_peak.yaml`

**Diseño "literal completo + ablaciones"** (spec §4 Fase 1 y §5): el brazo `literal` enciende los cuatro factores acoplados que el paper pide y que el pipeline no cumple. Cada ablación apaga **uno** de ellos para medir cuánto aporta dentro del conjunto. No se apilan factores de a uno (S138: D22 solo no devuelve nada) ni hay un brazo con `keep_peak` apagado solo (spec §6).

| brazo | D22 sin compuerta | D25 fondo por vecinos | D2 segundo pase condicionado | D19 `keep_peak` | responde |
|---|---|---|---|---|---|
| `control` | off | off | off | **on** | producción de hoy, reprocesada con el mismo código |
| `literal` | **on** | **on** | **on** | off | fidelidad literal en los cuatro puntos |
| `lit_sin_fondo` | on | off | on | off | ¿cuánto hace el fondo dentro del literal? |
| `lit_con_compuerta` | off | on | on | off | ¿cuánto hace la compuerta? |
| `lit_sp_suelto` | on | on | off | off | ¿cuánto hace condicionar el segundo pase? |
| `lit_keep_peak` | on | on | on | **on** | ¿cuánto hace `keep_peak`? |

En todos: `enable_tests_23_prose_branch` en su valor operacional (false, conectiva `min`, S136 abierta), `enable_local_cluster_magnitude_viirs375` en false (corona Eq.6, pisaría `pc.vrp_mw`), `sensors` sólo `viirs_375` (`run_pipeline.py:213-218` respeta esto).

- [ ] **Paso 1: correr el test de brazos y ver el rojo**

```bash
python -m pytest tests/test_flags_d22_d25_perfil_s142.py -k brazo -v
```

Esperado: 6 FAIL `falta el perfil ... (Tarea 7)` y `test_ningun_brazo_apaga_keep_peak_solo` PASS.

- [ ] **Paso 2: crear los seis perfiles**

`pipeline/profiles/_s142_ab_control.yaml`:

```yaml
# A/B S142, Fase 1 VIIRS 375: literal completo + ablaciones. Brazo CONTROL.
#
# EL FENÓMENO. En un cono nevado, de noche, el cráter con lava sub-píxel es más frío que el valle.
# Cuatro reglas del pipeline, acopladas, deciden si se ve y con cuánto: la compuerta de temperatura
# de los Tests 2 y 3 (D22), el fondo del VRP como mediana del anillo regional (D25), el segundo pase
# que corre sin detección previa (D2) y keep_peak (D19). Quitar una sola no mide nada (S138).
#
# Este brazo es la producción de hoy, reprocesada con el mismo código que los demás.
# CRITERIO: se pre-registra ANTES de correr (tarea siguiente del plan S142), con verificador limpio.
# NO OPERACIONAL: data_subdir aislado, fuera del cron.
extends: mirova_equivalent
profile: _s142_ab_control
description: >
  Brazo control: flags de producción; sólo VIIRS 375.

sensors:
  modis: false
  viirs_375: true
  viirs_750: false

output:
  data_subdir: _s142_ab_control
```

`pipeline/profiles/_s142_ab_literal.yaml`:

```yaml
# A/B S142, Fase 1 VIIRS 375: literal completo + ablaciones. Brazo LITERAL.
#
# Enciende los cuatro puntos donde el pipeline no sigue a Coppola 2016a: sin compuerta de
# temperatura en los Tests 2 y 3 (D22, p. 7), fondo = media de radiancia de los vecinos no
# alertados (D25, ec. 6), segundo pase sólo si hubo detección y sólo en la vecindad (D2), y sin
# keep_peak (D19). Ver docs/superpowers/plans/2026-09-15-flags-d22-d25.md §3 Tarea 7.
# NO OPERACIONAL: data_subdir aislado, fuera del cron.
extends: mirova_equivalent
profile: _s142_ab_literal
description: >
  Brazo literal: D22 on, D25 on, segundo pase condicionado on, keep_peak off; sólo VIIRS 375.

paths:
  enable_tests_23_no_bt_gate_viirs375: true
  enable_vrp_bg_neighbor_mean_viirs375: true
  enable_second_pass_conditioned: true
  enable_test1_contextual_keep_peak: false

sensors:
  modis: false
  viirs_375: true
  viirs_750: false

output:
  data_subdir: _s142_ab_literal
```

`pipeline/profiles/_s142_ab_lit_sin_fondo.yaml`:

```yaml
# A/B S142, Fase 1 VIIRS 375. Ablación del literal: SIN fondo por vecinos (D25 off).
# Mide cuánto aporta el fondo dentro del conjunto literal. Resto igual a _s142_ab_literal.
# NO OPERACIONAL: data_subdir aislado, fuera del cron.
extends: mirova_equivalent
profile: _s142_ab_lit_sin_fondo
description: >
  Literal menos D25: D22 on, D25 off, segundo pase condicionado on, keep_peak off; sólo VIIRS 375.

paths:
  enable_tests_23_no_bt_gate_viirs375: true
  enable_vrp_bg_neighbor_mean_viirs375: false
  enable_second_pass_conditioned: true
  enable_test1_contextual_keep_peak: false

sensors:
  modis: false
  viirs_375: true
  viirs_750: false

output:
  data_subdir: _s142_ab_lit_sin_fondo
```

`pipeline/profiles/_s142_ab_lit_con_compuerta.yaml`:

```yaml
# A/B S142, Fase 1 VIIRS 375. Ablación del literal: CON la compuerta de temperatura (D22 off).
# Mide cuánto aporta quitar la compuerta cuando el segundo pase ya no rescata en vacío.
# NO OPERACIONAL: data_subdir aislado, fuera del cron.
extends: mirova_equivalent
profile: _s142_ab_lit_con_compuerta
description: >
  Literal menos D22: D22 off, D25 on, segundo pase condicionado on, keep_peak off; sólo VIIRS 375.

paths:
  enable_tests_23_no_bt_gate_viirs375: false
  enable_vrp_bg_neighbor_mean_viirs375: true
  enable_second_pass_conditioned: true
  enable_test1_contextual_keep_peak: false

sensors:
  modis: false
  viirs_375: true
  viirs_750: false

output:
  data_subdir: _s142_ab_lit_con_compuerta
```

`pipeline/profiles/_s142_ab_lit_sp_suelto.yaml`:

```yaml
# A/B S142, Fase 1 VIIRS 375. Ablación del literal: segundo pase SUELTO (D2 off).
# Mide cuánto aporta condicionar el segundo pase dentro del conjunto literal.
# NO OPERACIONAL: data_subdir aislado, fuera del cron.
extends: mirova_equivalent
profile: _s142_ab_lit_sp_suelto
description: >
  Literal menos D2: D22 on, D25 on, segundo pase condicionado off, keep_peak off; sólo VIIRS 375.

paths:
  enable_tests_23_no_bt_gate_viirs375: true
  enable_vrp_bg_neighbor_mean_viirs375: true
  enable_second_pass_conditioned: false
  enable_test1_contextual_keep_peak: false

sensors:
  modis: false
  viirs_375: true
  viirs_750: false

output:
  data_subdir: _s142_ab_lit_sp_suelto
```

`pipeline/profiles/_s142_ab_lit_keep_peak.yaml`:

```yaml
# A/B S142, Fase 1 VIIRS 375. Ablación del literal: CON keep_peak (D19 on).
# Mide cuánto aporta quitar keep_peak cuando los otros tres puntos ya son literales.
# NO OPERACIONAL: data_subdir aislado, fuera del cron.
extends: mirova_equivalent
profile: _s142_ab_lit_keep_peak
description: >
  Literal menos D19: D22 on, D25 on, segundo pase condicionado on, keep_peak on; sólo VIIRS 375.

paths:
  enable_tests_23_no_bt_gate_viirs375: true
  enable_vrp_bg_neighbor_mean_viirs375: true
  enable_second_pass_conditioned: true
  enable_test1_contextual_keep_peak: true

sensors:
  modis: false
  viirs_375: true
  viirs_750: false

output:
  data_subdir: _s142_ab_lit_keep_peak
```

- [ ] **Paso 3: correr en verde**

```bash
python -m pytest tests/test_flags_d22_d25_perfil_s142.py -v
python -m pytest tests/test_gr2_profile_invariants.py tests/test_second_pass_conditioned_s135.py -q
```

Esperado: todo PASS (6 brazos, operacional, sección YAML, `max_half`, `keep_peak` solo, verificación; GR2 y S135 intactos).

- [ ] **Paso 4: suite completa**

```bash
python -m pytest -q -p no:cacheprovider 2>&1 | tail -3
```

Esperado: línea base + todos los tests nuevos PASS, 0 failed nuevos.

- [ ] **Paso 5: commit**

```bash
git add pipeline/profiles/_s142_ab_*.yaml
git commit -m "S142: perfiles del A/B Fase 1 V375, literal completo + 4 ablaciones + control

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tarea 8: documentación que el cambio exige

**Files:**
- Modify: `docs/MIROVA_DIVERGENCES.md` (entradas D22 y D25), `docs/FICHA_SDA_VRP_CHILE.md` (tabla de versiones)

- [ ] **Paso 1: D22**

En `docs/MIROVA_DIVERGENCES.md`, justo antes de la línea `**Lo que no se sabe**: cuántos falsos positivos devuelve quitarla, en los negativos del apéndice y en` (dentro de `## D22`), insertar:

```markdown
**S142, flag (OFF en producción).** `ENABLE_TESTS_23_NO_BT_GATE_VIIRS375` (`pipeline/profile.py`, sección `paths:`) quita la compuerta en VIIRS 375 en tres helpers (`contextual_dnti_hot_mask`, `dual_roi_contextual_dnti_hot_mask`, `first_pass_tests_2_and_3`, parámetro `apply_bt_gate`) y en el camino ETI (inerte). **No** la quita del camino B (NTI > K1, D23) ni en MODIS o VIIRS 750. Test sintético de punta a punta: sola, mueve los píxeles del segundo pase al primero y no cambia lo publicado (confirma S138). Brazos: `pipeline/profiles/_s142_ab_*.yaml`. Plan: `docs/superpowers/plans/2026-09-15-flags-d22-d25.md`.

```

- [ ] **Paso 2: D25**

En `docs/MIROVA_DIVERGENCES.md`, justo antes de la línea que empieza `**Fenómeno**: en una cumbre helada el cráter con lava sub-píxel está más frío en MIR` (dentro de `## D25`), insertar:

```markdown
**S142, flag (OFF en producción).** `ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375` calcula, en VIIRS 375, el fondo de cada píxel alertado como la media de la **radiancia** de sus vecinos no alertados (`pipeline/vrp_regimes.py:neighbor_mean_radiance_background`). Cuando los 8 vecinos están alertados, la ventana crece hasta `vrp_bg_neighbor_max_half_px` (3 = 7x7, decisión abierta), y si aun así no hay vecinos, se usa el fondo de hoy como respaldo, contado en `diag_bg_vecinos_n_sin_vecinos`. Entra en el bloque contextual (`anomaly_pixels`, cúmulo, F5) y en los dos recomputes del Test 1. Pisa al kernel 3x3 opt-in. El recorte a cero **no** cambia. MODIS y VIIRS 750 siguen con la mediana. En la escena sintética del plan, el cráter pasa de 0,0 a ~0,097 MW. Plan: `docs/superpowers/plans/2026-09-15-flags-d22-d25.md`.

```

- [ ] **Paso 3: FICHA SDA**

En `docs/FICHA_SDA_VRP_CHILE.md`, insertar como primera fila de la tabla (debajo de `|---|---|---|---|`, `:47`):

```markdown
| v1.6 | 2026-09-15 | **Sin cambios en la lógica de producción.** Se incorporan, desactivadas, dos alternativas metodológicas para el sensor VIIRS de 375 m, con el fin de evaluarlas con un experimento controlado antes de cualquier adopción: (1) quitar una condición de temperatura mínima que el método publicado por el grupo MIROVA no contiene en sus pruebas de contraste espacial, y que en volcanes con cumbre nevada puede descartar un foco débil real; (2) estimar la radiación de fondo de cada píxel anómalo a partir de sus píxeles vecinos no anómalos, como describe el método publicado, en lugar de un anillo regional de 5 a 25 km que en cumbres nevadas sobreestima el fondo y lleva a cero la energía de focos débiles. Ambas quedan apagadas en el perfil operacional; un control automático verifica que, apagadas, el resultado es idéntico bit a bit al anterior, y que no alteran los sensores MODIS ni VIIRS de 750 m. | S142 |
```

(La cabecera de la ficha, en `:7`, declara la versión v1.4 del 2026-08-30, pero la tabla ya tiene una fila v1.5. La inconsistencia es previa a este plan: avisarle a Nicolás y no corregirla sin su visto bueno, porque la ficha es un documento publicable.)

- [ ] **Paso 4: comprobar guiones y commit**

```bash
git diff docs/ > "$TMP/s142_docs.diff"
python -c "import os; t=[l for l in open(os.path.join(os.environ['TMP'],'s142_docs.diff'),encoding='utf-8') if l.startswith('+')]; print(sum(chr(0x2014) in l for l in t), sum(chr(0x2013) in l for l in t))"
```

Esperado: `0 0` (ninguna línea agregada con guion largo ni medio).

```bash
git add docs/MIROVA_DIVERGENCES.md docs/FICHA_SDA_VRP_CHILE.md
git commit -m "S142: D22 y D25 en el catálogo y FICHA v1.6 (flags OFF)

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tarea 9: citas `archivo:línea` corridas (CLAUDE.md, con visto bueno)

**Files:**
- Modify: `CLAUDE.md:813` (sólo con el visto bueno explícito de Nicolás)

- [ ] **Paso 1: medir las líneas nuevas**

```bash
grep -n "compute_test1_nti" pipeline/process_viirs.py | head -3
grep -n "FLAG_DNS = " pipeline/process_viirs.py
python -m pytest tests/test_guard_declarado_vs_efectivo_s131.py -q
```

Esperado: `FLAG_DNS` sigue en 83 y el import de `compute_test1_nti` sigue en 212 (el bloque nuevo va después de 215). G8 PASS. La llamada `test1_res = compute_test1_nti(` ya no está en 1082.

- [ ] **Paso 2: pedir el visto bueno y corregir la cita**

Con el número medido (llamémoslo `NUEVA`), mostrarle a Nicolás el cambio de `CLAUDE.md:813`: de `process_viirs.py:212/1082` a `process_viirs.py:212/NUEVA`, agregando al paréntesis «era 1082 hasta S142». Aplicarlo sólo si dice que sí.

- [ ] **Paso 3: comprobar G8b y commit**

```bash
python -m pytest tests/test_guard_declarado_vs_efectivo_s131.py -q
git add CLAUDE.md
git commit -m "S142: corre la cita process_viirs.py de A69 tras el cableado D22/D25

Co-Authored-By: Claude Opus 5 <noreply@anthropic.com>"
```

---

### Tarea 10 (siguiente, NO de este plan): pre-registro y verificador antes de correr el A/B

No escribir el workflow ni el pre-registro en este plan. Antes de despachar cualquier reproceso con los brazos de la Tarea 7:

1. Escribir `docs/PREREGISTRO_AB_D22_D25_S142.md` siguiendo `docs/PREREGISTRO_AB_D1_D2_S135.md` y los criterios del spec §5 (0 noches con alerta perdidas por estrato focal/nevado; baja la publicación en negativos limpios con bootstrap que excluye cero; magnitud más cerca de 1 sin empeorar más de 0,05 en volcanes con n ≥ 30; desarrollo enero-mayo y prueba junio-septiembre; cobertura pareja; anotación en `docs/HYPOTHESIS_LOG.md`), y el tamaño mínimo viable del spec §6 (6 volcanes, junio a agosto).
2. Hacerlo revisar por un verificador con contexto limpio antes de correr.
3. Recién entonces clonar el workflow (patrón `.github/workflows/reproc-s135-ab-d1d2.yml`, que incluye el paso "Verificar que el brazo LEE lo que declara") con `"on":` entre comillas (A43), un volcán por job y sin paralelo sobre el mismo `data_subdir` (A47).

---

## 4. Verificación final (antes de abrir PR)

- [ ] `python -m pytest -q -p no:cacheprovider 2>&1 | tail -3`: línea base + nuevos, 0 failed nuevos.
- [ ] `VRP_PROFILE=mirova_equivalent python -c "import pipeline.profile as p; print(p.ENABLE_TESTS_23_NO_BT_GATE_VIIRS375, p.ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375, p.VRP_BG_NEIGHBOR_MAX_HALF_PX)"` → `False False 3`.
- [ ] `python -m pytest tests/test_apagado_no_cambia_nada_s142.py -v`: 4 PASS.
- [ ] `PYTHONIOENCODING=utf-8 python experiments/_s133/auditar_guards_por_subcadena.py | tail -2` → `candidatos a falso verde: 0`, y después `git checkout -- experiments/_s133/auditar_guards_por_subcadena.json`.
- [ ] `python -c "t=open('docs/superpowers/plans/2026-09-15-flags-d22-d25.md',encoding='utf-8').read(); print(t.count(chr(0x2014)), t.count(chr(0x2013)))"` → `0 0`.
- [ ] Ningún archivo bajo `data/` modificado: `git diff --stat origin/main -- data/` vacío.
- [ ] El PR es de alta criticidad (pipeline operacional, A39/A45): pedir confirmación explícita de Nicolás antes del merge aunque CI esté verde.

---

## 5. Decisiones abiertas

1. **Píxel sin vecinos no alertados** (interior de un cúmulo o de una colada). El paper no lo trata. **Propuesta**: agrandar la ventana hasta 7x7 (`vrp_bg_neighbor_max_half_px: 3`) y, si aun así no hay vecinos, usar el fondo de hoy contado en el record. **Por qué**: para un cuerpo de varios píxeles, los no alertados más cercanos son su corona, que es lo que describen los textos al hablar de los píxeles que rodean al alertado o al cúmulo ("the pixels surrounding the alerted one(s)", tal como lo cita el docstring de `detection_context.py:197-199`; no releí Campus 2024 en esta sesión: SOSPECHA de redacción exacta). **Alternativas y su costo**: (a) sólo los 8 vecinos y respaldo directo (`max_half_px: 1`): el interior vuelve al anillo regional, que es justo D25; (b) exceso cero para el interior: subestima la magnitud en la fase efusiva, que es donde más importa (análogo a D23); (c) NaN: rompe sumas. El parámetro permite que el A/B barra 1 contra 3 sin tocar código.
2. **Media de radiancia, no de temperatura.** Propuesta: radiancia, porque así lo escriben Fernandina 2025 y Campus 2024 según `docs/MIROVA_DIVERGENCES.md:2299-2301`. Difiere del kernel S61, que promedia BT. En la escena sintética la diferencia es de ~4e-6 W/m2/sr/µm, pero crece con la heterogeneidad del entorno.
3. **Quiénes cuentan como "alertados" para el fondo del camino Test 1**: propuesta `hot_mask_2d | test1_hot_filtered`. Otra lectura válida es sólo `test1_hot_filtered`. Sin cita del paper, porque el Test 1 integrado no es de SP426.5 (D23).
4. **Precedencia sobre el kernel opt-in**: con el flag ON, pisa al kernel en los 5 volcanes opt-in. Si el A/B mostrara que Tupungatito (A19, glaciar) empeora, eso se investiga, no se parcha per-volcán (MISSION, trap S99).
5. **Camino ETI (`process_viirs.py:1180-1181`)**: se incluye en D22 por coherencia, pero hoy es inerte. Alternativa: dejarlo fuera y documentarlo.
6. **Recorte a cero del exceso negativo**: no se toca (pregunta 4 del correo a Coppola).
7. **Conectiva de los Tests 2 y 3 (`min` contra `max`)**: fuera de este A/B; todos los brazos quedan en `min` (S136, abierta).

---

## 6. SOSPECHA (no verificado en la sesión que escribió el plan)

- Que el Test 1 del paper (NTI > K1) tampoco tenga condición de temperatura. No leí esa página; por eso el camino B queda fuera de D22.
- La redacción exacta de Coppola 2016a ec. 6, Fernandina 2025 p. 9 y Campus 2024 p. 3: la tomé del catálogo (`docs/MIROVA_DIVERGENCES.md:2297-2301`) y de comentarios del código, no de las páginas renderizadas.
- Que `hot_mask_2d` esté siempre definido en los bloques del Test 1 (razonamiento en Tarea 6, Paso 9; se verifica con la suite).
- Los valores ON de extremo a extremo de D22 salen de un probe que **simuló** la compuerta poniendo `NTI_BT_SANITY_K = -1e9`, lo que también movió los caminos B y C. Los de D25 salen de simular con el kernel BT existente más un cálculo a mano de la media de radiancia. Los tests se escribieron para que fallen si la escena no reproduce lo esperado (Tarea 1, Paso 2).
- El conteo de passed de la suite: al escribir el plan sólo se midió la recolección (1451) y que 48 tests de S135, GR2 y kernel local pasan. La línea base real se toma en la Tarea 0.
- Costo de CPU del bucle por píxel del helper en records eruptivos (miles de píxeles, ventana hasta 7x7): no medido. Si un job NRT se acercara al timeout con el flag ON, vectorizar antes de adoptar (hoy no corre en NRT).
- Que en el arnés el camino Test 1 no dispare en ninguna escena (`final_hotspot_source` fue `ctx_cluster` en la escena principal). Por eso D25 sobre el Test 1 queda cubierto sólo por guards de fuente, no por un caso sintético de punta a punta.

---

## 7. Autorrevisión del plan

- **Cobertura del pedido.** Header y §0 en lenguaje de geólogo: sí. Mapa de archivos con `archivo:línea`: §1 y §2. Tag exacto: Tarea 0. TDD por flag: (a) apagado bit a bit, Tareas 1, 2 y 6; (b) encendido cambia exactamente lo dicho: D22 en Tareas 3 y 6, D25 en Tareas 5 y 6; (c) perfil operacional en False y sección correcta: Tarea 4 más GR2; (d) MODIS y V750 no cambian: Tarea 2 con el perfil de verificación, más guards de fuente en la Tarea 6. Comandos con salida esperada y suite completa: en cada tarea. Brazos "literal completo + ablaciones" con `enable_second_pass_conditioned` y test de lectura: Tarea 7. Workflow y pre-registro: excluidos, Tarea 10. Documentación: FICHA, D22, D25, cabecera de `vrp_regimes.py`, comentarios POR QUÉ en cada bloque. Decisiones abiertas y SOSPECHA: §5 y §6.
- **Placeholders.** Ninguno de código. El único valor que no se puede escribir de antemano es el número de línea nuevo de `compute_test1_nti(` en la Tarea 9: se mide con un comando y se aplica con visto bueno.
- **Consistencia de nombres** (revisados uno por uno): `ENABLE_TESTS_23_NO_BT_GATE_VIIRS375` / `enable_tests_23_no_bt_gate_viirs375`; `ENABLE_VRP_BG_NEIGHBOR_MEAN_VIIRS375` / `enable_vrp_bg_neighbor_mean_viirs375`; `VRP_BG_NEIGHBOR_MAX_HALF_PX` / `vrp_bg_neighbor_max_half_px`; `apply_bt_gate`; `neighbor_mean_radiance_background(bt_grid, alert_mask, hot_rows, hot_cols, wavelength_um, *, max_half_px)` → `(l_bk, half_used)`; `vrp_bg_neighbor_mean_v375(bt_grid, alert_mask, hot_rows, hot_cols, legacy_l_bg, *, max_half_px)` → `(l_bg, n_sin_vecinos)`; campos `diag_L_bg_vecinos_w_m2_sr_um` y `diag_bg_vecinos_n_sin_vecinos`; `_eti_gate_bt`; `_t1_Lbg`; perfiles `_s142_verif_flags_nuevos` y `_s142_ab_{control,literal,lit_sin_fondo,lit_con_compuerta,lit_sp_suelto,lit_keep_peak}`; arnés `correr_en_subproceso(tmp_dir, perfil, parches, solo)`, `salidas_helpers(**extra)`, `entradas_helpers()`, `escena_v375(tipo)`, `CENTRO_V375`, `N_V375`.
- **Errores corregidos durante la revisión.** (1) El primer borrador ponía como `old_string` de D25 una línea del código que tiene un guion largo; ahora el reemplazo se ancla en la línea única `L_bg = bt_to_spectral_radiance(np.float64(t_bg_i04), I04_LAMBDA)`, y así el plan no contiene guiones largos. (2) El primer borrador afirmaba que `_tokens` no cuenta una expresión con espacios y le ponía `== 0`. Era falso: la frontera de palabra sí la encuentra (comprobado con un ejemplo corrido). El guard quedó en `== 3`.
