# VRP-Chile — profile: _t1lm_q0_control (S112 Parte B, brazo Q0 CONTROL)
#
# Baseline OPERACIONAL re-procesado con el código actual (Parte A OFF, sin brazos S112).
# Es el control apples-to-apples: cuánta magnitud da HOY el pipeline en las noches ALERTA
# (esperado ~0 por la cascada de source) y cuánta RUTINA marca. NO usar el JSON live como
# baseline (escrito con código de fechas distintas; A62). data_subdir aislado (A47).
extends: mirova_equivalent
profile: _t1lm_q0_control
description: >
  A/B S112 Test1-lowmag — Q0 control (operacional actual, Parte A OFF).
output:
  data_subdir: _t1lm_q0_control
