# Handoff S39 → S40 (2026-05-13+)

**Estado al cierre S39**: D4 per-volcano implementado (Lascar + Lastarria),
A/B run 25797247811 corriendo (ETA ~1.5h). Operacional S38 (vent_anchored + H8)
en main. Filtro UI "🆕 Solo post-S38" publicado en GitHub Pages.

## Bloque A — Verificar A/B D4 per-vol cuando termine

Comando: `python experiments/87_audit_d8_combo_ab.py` adaptado para
`_d8_d4_per_vol` vs `mirova_equivalent` baseline.

Criterios:
- Recall global >= 89.5% (no perder vs operacional S38)
- Lascar: +X TP (esperado +1-3 por D4 fix recovery)
- Lastarria: +X TP (esperado +1-2)
- Tupungatito: **+0 esperado** (lbg_global_compatible=false, no afectado)
- Planchón: **+0 esperado** (igual)
- Otros: +0 (lbg_global_compatible default false)

## Bloque B — Adopción D4 per-vol si A/B valida

1. R5 brainstorming corto (R5/R6 trivial: cambio gated por field per-vol,
   reversible quitando field)
2. Modificar `mirova_equivalent.yaml`: `enable_test1_lbg_global: true`
3. Push a main → próximo cron NRT 2h aplica
4. R8 verificación operacional

## Bloque C — Agregar NdC al D4 per-vol (S40)

Análisis S39 reveló: NdC tiene 2/3 alertas con patrón D4 idéntico a
Lascar/Lastarria (Test 1 dispara con 49-88 pixels @ 0.7-1.8km del vent,
vrp=0). NdC tiene fumarolas moderadas — posiblemente apto para D4 fix.

NO incluido en A/B actual (ya corriendo). Si Lascar+Lastarria A/B
valida en B, agregar NdC a volcanoes.yaml y re-A/B incremental
solo en NdC. Si suma TP sin regresar → adoptar.

Pendiente: 3ra alerta NdC 2026-04-20 (VIIRS 750m, 0.99MW @ 3.35km) —
verificar si hay granule disponible. Si NO se procesó, no es D4.

## Bloque D — A/B incremental cleanup paths viejos

Análisis empírico S39 (records operacional 30d):
- `bt_path_hot` solo: **0 records contribuye exclusivamente** en 10/11 vol
  (solo 6 en Villarrica). Candidate a retirar.
- `test1_hot` solo: 848 records (path dominante).
- `dnti_ctx_hot` contribuye: 976 records (load-bearing).
- Mixed: 8 records.

Plan A/B:
1. Profile flag `ENABLE_BT_PATH_HOT` (default true backward-compat)
2. En procesadores: `bt_hot_path & ENABLE_BT_PATH_HOT` o `np.zeros_like`
3. Perfil `_no_bt_path.yaml` con flag OFF
4. A/B 15d comparar vs operacional
5. Si recall delta < -2pp: retirar bt_path del clon literal

Estimación: 1h dev + 1.5h A/B. Si valida, simplifica código + acerca a
clon literal MIROVA (paper Coppola NO usa bt_path puro).

## Bloque E — Investigación profundización (S40-S41)

### Villarrica ratio inflado
1 alerta MIROVA 0.31 MW @ 0.84km, nuestro vrp_mw=5.837 (ratio 18.8×).
primary cluster correcto @ 0.16km (vent_anchored funciona). Pero suma de
pixels es alta. ¿Lava lake real es 0.31 MW pero capturamos pixels alrededor
del lago + microclima? Pixel-level R2 vs TIF MIROVA cerraría duda.

### NdC alerta VIIRS 750m faltante
2026-04-20 19:00:02 NdC 0.99 MW. Probable granule no procesado por
diurno-filter (19:00 GMT = 16:00 local Chile = tarde, posiblemente filtro
night). Verificar process_viirs_mod.py `_check_night` lógica.

### Frontend toggle Post-S38 verificación visual
GitHub Pages publicó (2026-05-13 11:36 success). Test plan en
`docs/superpowers/specs/2026-05-13-frontend-audit-s38.md`:
1. Activar toggle → records pre-2026-05-13 desaparecen
2. Persistencia sessionStorage
3. Cross-page sincronización
4. Volcán sin data post-S38 → card vacío
5. Charts/tabla/mapa filtrados

## Estado tests al cierre S39

Suite: 266 passed, 16 skipped, 0 xfailed. +6 nuevos tests D4 per-vol.

## Resumen ejecutivo S37 → S38 → S39

| Sesión | Hipótesis | A/B resultado | Adopción |
|---|---|---|---|
| S37 H_D8_5 | "MIROVA suma todo, primary es invento" | +0 TP, REFUTADO | No |
| S38 vent_anchored | "MIROVA elige cluster vent-anchored" | +11 TP 15d, +16 TP 30d | **SÍ** (PR #28) |
| S38 C.1 combo D4 | "D4 fix universal mejora" | -2 TP regresión Tupungatito+Planchón | No (universal) |
| S39 D4 per-vol | "D4 selectivo Lascar+Lastarria" | EN CURSO | Pendiente A/B |
| S40+ Bloque D | "bt_path retirable" | Pendiente | Pendiente |
