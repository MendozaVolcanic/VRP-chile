# Handoff S47 — Cierre S46 + roadmap S47

S46 cerrada **2026-05-16**. Sesión maratón con plan documentado, 13 tasks
implementadas (TDD + reviews), 2 rondas A/B (143+99 jobs), 1 adopción
operacional (drift234), 1 bug crítico detectado y fixed.

---

## ⭐ HALLAZGOS PRINCIPALES S46

### 1. Adopción operacional drift234 ✅

`mirova_equivalent.yaml` ahora activa:
- `enable_first_pass_tests_2_and_3: true` (Tests 2 ∧ 3 + dETI Coppola 2016a)
- `enable_dual_roi_first_pass: true` (Tabla 2 summit/scene)
- `enable_second_pass_adjacent: true` (paso 5 paper)
- `enable_dual_roi_second_pass: true`

Métricas reales (window 30d 2026-04-16→2026-05-16, CSV fresh):
- **F1 89.2% → 89.9%** (+0.69pp)
- **MODIS recall 92.3% → 100%** (recupera 3 FN Lascar)
- **Ratio mediano VRP 1.72× → 1.21×** (mejor paridad MIROVA)
- **Precision intacta** 84.6% → 84.8%

Mecanismo: second-pass adyacente recompute background sin pixels active
→ recapture cluster cráter cuando primary era Salar Atacama lejano
(resuelve D9 summit-priority S45).

### 2. Bug crítico audit script DETECTADO Y FIXED

`_vrp_mirovaEq` línea 101: `if record.get("distance_class") == "far": return 0.0`
filtraba records donde `primary_cluster.centroid_dist <= inner_radius` pero
`hotspot_dist_km` (legacy) era far. Sub-contaba TPs ~70% en Lascar MODIS.

Pre-fix audit: MODIS F1=32% (4/21 alertas). Post-fix: MODIS F1=92.3% (18/21).
"Gap MODIS" era 100% artefacto del bug.

Commit fix: `12aa4b2`.

### 3. Ronda 2 — Ninguna variante mejora vs drift234

9 variantes exploraron paramétricas (C1/C2 overrides) y arquitecturales
(uniform_no_dual, drift4_alone, per-sensor). Resultado: **drift234 es
óptimo**.

Lecciones:
- Paper Coppola C1=0.003 / C2=5 son buenos defaults (no mejorables paramétricamente)
- Dual-ROI Tabla 2 es **operacionalmente transparente** en volcanes chilenos
- Filtros Coppola más estrictos (drift23, dibella n=12) PIERDEN alertas legítimas
- **MIROVA NRT real es más permisivo que paper publicado** — parches S33-S44 ya
  capturan lo que paper estricto haría

### 4. Falso "WIN" preview con data parcial (lección importante)

2 variantes mostraron preview "WIN +1.2pp F1" con datos parciales (8-9/11 vol),
que se desplomaron a +0.0 al completar 11/11:
- `r2_uniform_no_dual`: preview 91.1% → final 89.9%
- `r2_drift4_alone`: preview 91.1% → extrapolado 89.9%

**Regla S47+**: NUNCA decidir adopción basado en data parcial. Esperar 11/11
o ratio comparable.

---

## 📊 ESTADO MODELO OPERACIONAL POST-S46

Pipeline `mirova_equivalent.yaml`:
```yaml
enable_vent_anchored_clustering: true       # S38 H8
enable_pixel_level_distance_filter: true    # S38 H8
enable_test1_lbg_global: true               # S39 D4 per-vol
enable_bt_path_hot: false                   # S40
enable_first_pass_tests_2_and_3: true       # S46 R1 ADOPTED
enable_dual_roi_first_pass: true            # S46 R1 ADOPTED
enable_second_pass_adjacent: true           # S46 R1 ADOPTED
enable_dual_roi_second_pass: true           # S46 R1 ADOPTED
# Sanity cap pc.vrp_mw=50,000 hardcoded store.py (S41)
# final_hotspot_source=test1 cuando única (S44)
```

Métricas post-adopción (verificable):
- F1 89.9%, Recall 95.6%, Precision 84.8%
- MODIS F1=92.3% (con drift234)
- VIIRS M F1=93.8%
- VIIRS375 F1=87.8%
- Ratio mediano VRP 1.21× (vs MIROVA)
- Window 30d, 11 Tier A

---

## 🔬 ROADMAP S47

### Prioridad ALTA

1. **Investigar FPs distribución** (39 FPs sobre 256 detecciones = 15%)
   - ¿En qué volcanes? Per-vol breakdown
   - ¿FALSO_POSITIVO MIROVA o "ALERTA_TERMICA no en CSV"?
   - Si segundo: probable gap CSV scraper
   - Si primero: drift residual no identificado

2. **Verificar adopción drift234 NRT cron** (verificación R8 post-deploy)
   - Próximos 1-2 ciclos cron deberían reflejar drift234 active
   - Comparar data NRT post-adopción vs pre-S46
   - Confirmar 0 regresión en producción

3. **Pixel-level R2 sistemático** (tests/test_r2_pixel_level.py)
   - Casos canónicos con TIF disponible
   - Confirmar pixel cráter coincide con MIROVA TIF

### Prioridad MEDIA

4. **Refresh dashboard frontend con métricas S46**
   - Update "Resumen ejecutivo" tab si existe
   - Comparar baseline (pre-S46) vs operacional (drift234)

5. **Audit per-sensor profundo VIIRS375**
   - F1=87.8% mejor que MODIS pero 39 FPs significativos
   - VIIRS 375m tiene más resolución → más sensible → más ruido?

### Prioridad BAJA

6. **Limpieza variantes A/B Ronda 1+2** (housekeeping)
   - 22 dirs `data/_*/` ocupan espacio repo
   - Considerar archivar a branch `s46-ab-data` y borrar de main

7. **Variantes Ronda 3** (si MODIS gap o FPs sugieren patrón)
   - Combos C1+C2 simultáneos
   - C2 per-sensor distinto (MODIS vs VIIRS)
   - drift234 + sanity cap pc.vrp_mw distintos thresholds

---

## 🛠️ CÓDIGO S46 PERSISTIDO

Pull requests + commits clave:
- PR #41 merged (`958a7a1`): docs S45 + implementación S46 13 tasks
- `12aa4b2` audit bug fix `distance_class` filter
- `3d25ea1` adopción operacional drift234
- `4840db1` Ronda 2 código + 9 profiles + workflow + audit
- `aa0a6c3` Ronda 2 audit final

Branch `claude/sweet-austin-b5413b` cherry-picked y pushed a main.
Sin PR abierto.

22 profile YAMLs en `pipeline/profiles/_*.yaml` (13 R1 + 9 R2). Default OFF,
opt-in via `data_subdir`. Operacional `mirova_equivalent.yaml` con drift234.

Audit script `experiments/87_audit_s46_round1.py` reutilizable para
futuros A/B. Output `experiments/87_results.{md,json}`.

Tests sintéticos 298/21 verde post-S46.

---

## 📌 PRIMER MENSAJE PRÓXIMA SESIÓN

Leer este handoff. Verificar primero:
1. NRT cron últimos runs usan drift234 (ver commits NRT)
2. F1 NRT post-adopción coherente con ~89.9% baseline S46
3. Ningún regreso operacional inesperado

Después decidir prioridad S47:
- A) Investigar 39 FPs distribución (probable gap CSV scraper, no bug)
- B) Pixel-level R2 con TIFs frescos
- C) Refresh dashboard con métricas S46
- D) Limpieza housekeeping

Plan disponible: `docs/superpowers/plans/2026-05-15-s46-coppola-literal-implementation.md`
Design doc: `docs/superpowers/specs/2026-05-15-s46-coppola-literal-design.md`
